id: group_data_permission
name: 用户组数据权限
table_name: group_data_permissions
description: 用户组的数据访问权限

fields:
  - id: id
    name: ID
    type: integer
    db_column: id
    required: true
    unique: true
    description: 技术主键

  - id: group_id
    name: 用户组ID
    type: integer
    db_column: group_id
    required: true
    description: 用户组ID

  - id: resource_type
    name: 资源类型
    type: string
    db_column: resource_type
    required: true
    description: 资源类型 domain/sub_domain/service_module/business_object

  - id: resource_id
    name: 资源ID
    type: integer
    db_column: resource_id
    required: true
    description: 资源ID

  - id: permission_level
    name: 权限级别
    type: string
    db_column: permission_level
    required: true
    description: 权限级别 read/write/admin

  - id: inherit_to_children
    name: 继承到子级
    type: boolean
    db_column: inherit_to_children
    default: true
    description: 是否自动继承到子层级

  - id: created_at
    name: 创建时间
    type: datetime
    db_column: created_at
    description: 创建时间

indexes:
  - fields: [group_id, resource_type, resource_id]
    unique: true
    name: idx_group_data_perm_unique
  - fields: [group_id]
    name: idx_group_data_perm_group

relations: []
actions: []

ui_view_config:
  skip_auto_menu: true

# ============================================
# 9 模式 UI 测试配置 (auto-completed by enrich_schemas.py)
# ============================================

ui_badge:
  inherit_to_children:
    color_map:
      true: success
      false: info

routes:
  detail: /detail/group_data_permission/group_data_permission-detail
  list: /group_data_permission-management

list:
  pagination:
    default_page_size: 20
    page_size_options: [10, 20, 50, 100]

nested:
  enabled: False

persistence:
  strategy: audit_log
  survive_reload: True
