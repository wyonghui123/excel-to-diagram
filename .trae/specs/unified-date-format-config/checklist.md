# Checklist: 统一日期格式配置

> **Spec**: [spec.md](./spec.md)
> **创建日期**: 2026-05-24
> **更新日期**: 2026-05-24

---

## Phase 1: 核心能力 (P0)

### 数据模型

- [ ] 在 `meta/schemas/user.yaml` 中新增 5 个偏好字段
  - locale (语言区域)
  - timezone (时区)
  - date_style (日期格式长度)
  - time_style (时间格式长度)
  - hour_cycle (时间制式)
- [ ] 运行 Schema 同步，数据库表新增对应列
- [ ] 验证现有用户自动填充默认值

### 后端服务

- [ ] 创建 `meta/services/date_format_service.py` 日期格式化服务
- [ ] 编写后端单元测试

### 前端服务

- [ ] 创建 `src/services/DateFormatService.js` 日期格式化服务
- [ ] 创建 `src/stores/userPreferences.js` Pinia Store
- [ ] 实现 `format()` 方法
- [ ] 实现 `formatDate()` 方法
- [ ] 实现 `formatTime()` 方法
- [ ] 实现 `detectTimezone()` 方法
- [ ] 编写前端单元测试

### 语言默认配置

- [ ] 定义中文（zh-CN）默认配置
- [ ] 定义美式英语（en-US）默认配置
- [ ] 定义英式英语（en-GB）默认配置

---

## Phase 2: 完整体验 (P1)

### 用户设置界面

- [ ] 在 `AccountSettings/index.vue` 新增"偏好设置"Tab
- [ ] 实现语言区域选择组件
- [ ] 实现时区选择组件（可搜索下拉框）
- [ ] 实现日期格式选择组件
- [ ] 实现时间格式选择组件
- [ ] 实现时间制式选择组件
- [ ] 实现实时预览功能
- [ ] 实现保存功能
- [ ] 编写 E2E 测试

### 时区自动检测

- [ ] 实现首次登录时自动检测时区
- [ ] 实现自动设置检测到的时区
- [ ] 用户可手动覆盖自动检测的时区

### 代码迁移

- [ ] 更新 `useMetaList.js` 中的 `formatDate` 函数
- [ ] 更新 `MetaListPage` 组件使用新服务
- [ ] 搜索并更新所有使用日期格式化的组件
- [ ] 标记旧 `formatDate` 函数为 deprecated
- [ ] 验证所有日期显示正常

---

## 验收标准

### 功能验收

- [ ] 用户可以设置语言区域
- [ ] 用户可以设置时区
- [ ] 用户可以设置日期格式长度
- [ ] 用户可以设置时间格式长度
- [ ] 用户可以设置时间制式
- [ ] 设置保存后立即生效
- [ ] 刷新页面后设置保持

### 国际化验收

- [ ] 中文环境下日期格式正确
- [ ] 美式英语环境下日期格式正确
- [ ] 英式英语环境下日期格式正确
- [ ] 12小时制显示正确
- [ ] 24小时制显示正确

### 性能验收

- [ ] 日期格式化操作耗时 < 1ms
- [ ] 无浏览器兼容性问题

---

## 风险检查

- [ ] 兼容层已实现，可随时回退
- [ ] 数据库新增列可删除
- [ ] 无安全漏洞引入
- [ ] 无性能退化
