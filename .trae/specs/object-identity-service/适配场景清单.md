# 对象标识转换服务适配场景清单

## 执行时间
2026-05-09

---

## 一、适配场景总览

| 序号 | 场景分类 | 具体场景 | 文件路径 | 优先级 | 影响范围 | 实施难度 | 状态 |
|------|----------|----------|----------|--------|----------|----------|------|
| 1 | 审计日志 | 审计日志 API | meta/api/audit_api.py | P0 | 高 | 低 | ⚠️ 待适配 |
| 2 | 审计日志 | 前端审计日志页面 | src/views/SystemManagement/AuditLogManagement.vue | P0 | 高 | 中 | ⚠️ 待适配 |
| 3 | 权限配置 | 条件规则对话框 | src/views/SystemManagement/ConditionRuleDialog.vue | P1 | 中 | 中 | ⚠️ 待评估 |
| 4 | 权限配置 | 角色权限中心 | src/views/SystemManagement/RolePermissionCenter.vue | P1 | 中 | 中 | ⚠️ 待评估 |
| 5 | 架构数据 | 详情面板 | src/views/ArchDataManageApp/components/DetailPanel.vue | P2 | 低 | 高 | ✅ 暂不处理 |
| 6 | 架构数据 | 动态视图 | src/views/ArchDataManageApp/components/DynamicView.vue | P2 | 低 | 高 | ✅ 暂不处理 |
| 7 | 导入导出 | 导入导出服务 | meta/services/import_export_service.py | P2 | 低 | 高 | ✅ 暂不处理 |
| 8 | 其他 | 用户管理 | src/views/SystemManagement/UserManagement.vue | P3 | 低 | 低 | ✅ 暂不处理 |
| 9 | 其他 | 角色详情 | src/views/SystemManagement/RoleDetailDrawer.vue | P3 | 低 | 低 | ✅ 暂不处理 |

---

## 二、P0 优先级场景详情

### 场景 1: 审计日志 API

**文件**: `meta/api/audit_api.py`

**现状分析**:
- 使用硬编码的 `BUSINESS_KEY_METADATA` 字典配置
- 使用 `_generate_business_key()` 函数生成业务键
- 仅用于审计日志展示

**问题清单**:
- ❌ 硬编码配置，非元数据驱动
- ❌ 功能单一，不支持层级路径
- ❌ 无法复用到其他场景

**适配方案**:
```python
# 迁移方案
from meta.services.object_identity_service import ObjectIdentityService

def _generate_business_key(data_source, object_type, object_id, ...):
    """
    @deprecated 使用 ObjectIdentityService 替代
    保留此函数以保持向后兼容
    """
    service = ObjectIdentityService(data_source)
    identity = service.get_identity(object_type, object_id, format='short')
    return identity.get('formatted', '')
```

**影响范围**:
- 审计日志列表展示
- 审计日志详情
- 审计日志导出

**测试要点**:
- [ ] 审计日志展示正确的对象标识
- [ ] 支持层级路径展示
- [ ] 性能满足要求（< 50ms）
- [ ] 向后兼容

---

### 场景 2: 前端审计日志页面

**文件**: `src/views/SystemManagement/AuditLogManagement.vue`

**现状分析**:
- 从后端获取 business_key 字段
- 直接展示，无前端格式化

**适配方案**:
```javascript
// 引入 Composable
import { useObjectIdentity } from '@/composables/useObjectIdentity'

const { batchGetIdentities } = useObjectIdentity()

// 在数据加载后获取对象标识
async function loadObjectIdentities(logs) {
  const requests = logs.map(log => ({
    object_type: log.object_type,
    object_id: log.object_id
  }))
  
  const identities = await batchGetIdentities(requests, { format: 'short' })
  
  // 更新 logs，添加 formatted_identity 字段
  logs.forEach(log => {
    const key = `${log.object_type}:${log.object_id}:short:false`
    log.formatted_identity = identities.get(key)?.formatted || log.business_key
  })
}
```

**测试要点**:
- [ ] 前端正确调用 API
- [ ] 缓存机制生效
- [ ] 错误处理正确
- [ ] 性能满足要求

---

## 三、P1 优先级场景详情

### 场景 3: 条件规则对话框

**文件**: `src/views/SystemManagement/ConditionRuleDialog.vue`

**现状分析**:
- 配置条件权限规则
- 可能涉及领域、子领域等对象

**待评估问题**:
- ⚠️ 规则的业务描述是否需要展示对象标识？
- ⚠️ 数据权限规则是否涉及对象层级？

**评估步骤**:
1. 分析条件规则的业务需求
2. 确认是否需要展示对象标识
3. 如需要，设计适配方案

---

### 场景 4: 角色权限中心

**文件**: `src/views/SystemManagement/RolePermissionCenter.vue`

**现状分析**:
- 展示角色描述（description）
- 展示菜单描述
- 展示服务动作权限

**待评估问题**:
- ⚠️ 权限规则的业务描述是否需要展示对象标识？
- ⚠️ 数据权限规则是否涉及领域、子领域等对象？

**评估步骤**:
1. 分析权限配置的业务需求
2. 确认是否需要展示对象标识
3. 如需要，设计适配方案

---

## 四、P2-P3 优先级场景详情

### 场景 5-6: 架构数据管理

**文件**: 
- `src/views/ArchDataManageApp/components/DetailPanel.vue`
- `src/views/ArchDataManageApp/components/DynamicView.vue`

**现状分析**:
- 展示 domain_name、sub_domain_name、service_module_name 等字段
- 通过 hierarchyPath 计算属性构建层级路径
- 这些字段由 enrichment_engine 填充

**架构对比**:

| 维度 | EnrichmentEngine | ObjectIdentityService |
|------|------------------|----------------------|
| 设计目标 | 查询时填充虚拟冗余字段 | 统一对象标识转换 |
| 实现方式 | 从 redundancy_registry 读取定义 | 从元数据读取 business_key 定义 |
| 适用场景 | 列表查询、详情展示 | 审计日志、通用标识转换 |
| 性能 | 批量查询优化 | 批量查询优化 + 缓存 |
| 层级路径 | 需要前端计算 | 后端直接返回 |

**建议**:
- **短期**: 保持现有架构，ObjectIdentityService 用于审计日志等新场景
- **中期**: 评估两者整合的可能性
- **长期**: 考虑统一对象标识服务

---

### 场景 7: 导入导出服务

**文件**: `meta/services/import_export_service.py`

**现状分析**:
- 使用 `parent_key_columns` 标识父对象业务键列
- 使用 `business_key` 字段进行唯一性校验
- 导入时解析父级上下文

**建议**:
- **不建议立即迁移**
- 导入导出服务有自己的业务逻辑（Excel 列、导入顺序等）
- 可以在后续版本中考虑复用 HierarchyPathService

---

### 场景 8-9: 其他前端页面

**文件**: 
- `src/views/SystemManagement/UserManagement.vue`
- `src/views/SystemManagement/RoleDetailDrawer.vue`

**现状分析**:
- 使用 display_name 字段展示
- 无业务键展示需求

**建议**:
- **暂不需要适配**

---

## 五、实施计划

### Week 1: P0 场景适配

#### Day 1-2: 审计日志 API 迁移
- [ ] 修改 `_generate_business_key()` 函数
- [ ] 标记为 `@deprecated`
- [ ] 内部调用 ObjectIdentityService
- [ ] 测试验证

#### Day 3-4: 前端审计日志页面集成
- [ ] 引入 useObjectIdentity Composable
- [ ] 修改数据加载逻辑
- [ ] 添加错误处理
- [ ] 测试验证

#### Day 5: 回归测试
- [ ] 审计日志功能测试
- [ ] 性能测试
- [ ] 兼容性测试

---

### Week 2: P1 场景评估

#### Day 1-2: 权限配置场景评估
- [ ] 分析条件规则对话框需求
- [ ] 分析角色权限中心需求
- [ ] 确定是否需要适配

#### Day 3-4: 如需适配，实施适配
- [ ] 设计适配方案
- [ ] 实施适配
- [ ] 测试验证

#### Day 5: 文档更新
- [ ] 更新使用文档
- [ ] 更新 API 文档
- [ ] 更新架构文档

---

### Week 3+: P2-P3 场景评估

#### 架构整合评估
- [ ] 评估 EnrichmentEngine 与 ObjectIdentityService 整合可能性
- [ ] 设计统一架构
- [ ] 逐步迁移

---

## 六、验收标准

### 功能验收
- [ ] 审计日志展示正确的对象标识
- [ ] 支持层级路径展示
- [ ] 权限配置展示正确
- [ ] 无功能回退

### 性能验收
- [ ] 单个对象查询 < 50ms
- [ ] 批量查询（100个对象）< 500ms
- [ ] 缓存命中率 > 80%

### 质量验收
- [ ] 所有单元测试通过
- [ ] 所有集成测试通过
- [ ] 代码覆盖率 > 80%
- [ ] 无 lint 错误
- [ ] 无类型错误

### 兼容性验收
- [ ] 旧代码仍然可用（标记废弃）
- [ ] 现有功能不受影响
- [ ] 渐进式迁移路径清晰

---

## 七、风险与缓解

### 高风险 ⚠️

**风险 1**: audit_api.py 迁移可能影响审计日志展示
- **缓解**: 保留旧函数，标记废弃，渐进式迁移
- **测试**: 完整的回归测试

**风险 2**: EnrichmentEngine 整合可能影响现有架构
- **缓解**: 充分评估，逐步迁移
- **测试**: 完整的功能测试

### 中风险 ⚠️

**风险 3**: 前端集成可能失败或性能问题
- **缓解**: 先在测试环境验证，监控性能
- **测试**: E2E 测试

### 低风险 ✅

**风险 4**: 核心服务可能有缺陷
- **缓解**: 完整的单元测试覆盖
- **测试**: 单元测试已通过

---

## 八、总结

### 关键发现

1. **EnrichmentEngine 已提供虚拟冗余字段填充能力**
   - 与 ObjectIdentityService 功能重叠
   - 现有架构已经很好地解决了层级路径展示问题

2. **权限配置场景需要进一步评估**
   - 规则的业务描述可能需要展示对象标识
   - 需要分析具体需求

3. **建议渐进式迁移**
   - 短期：双架构并行
   - 中期：评估整合
   - 长期：统一架构

### 下一步行动

1. ✅ 完成 P0 场景适配（审计日志）
2. ⚠️ 评估 P1 场景（权限配置）
3. ⚠️ 监控性能和用户反馈
4. ⚠️ 评估架构整合可能性
