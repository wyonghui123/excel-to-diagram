# 🤖 AI Generated Draft - needs_pm_review: true
# Review Actions: [Approve] [Request Changes] [Edit in IDE]
# Generated by: business-flow-planner
# Generated at: 2026-06-13T10:00:00Z
# Schema: .trae/specs/templates/business-flow.schema.json

# ==== PM/BA Review 状态 ====
review_status: draft  # draft | reviewed | approved
agent_draft: true

# ==== 业务流主体 ====

actor: Admin  # Admin | Readonly | BusinessAnalyst | DataSteward
goal: "{{ 业务目标,例如: 创建业务对象并验证 key_template 自动编码 }}"

# ==== 前置条件 ====
preconditions:
  - "{{ 前置条件 1,例如: admin 用户已登录,选定了产品+版本+领域+子领域+服务模块 }}"
  - "{{ 前置条件 2 }}"

# ==== 业务原子动作(非 UI 操作) ====
tasks:
  - id: T_BIZ_BO_001
    title: "{{ 任务 1,例如: 打开业务对象新建表单 }}"
    class: "{{ Screenplay Task 类名,如 OpenBusinessObjectForm }}"
    params:
      # 任务参数
    priority: P0  # P0 | P1 | P2
  - id: T_BIZ_BO_002
    title: "{{ 任务 2,例如: 填写业务对象名称 }}"
    class: FillBusinessObjectName
    params:
      name: "客户订单"
    priority: P0
  - id: T_BIZ_BO_003
    title: "{{ 任务 3,例如: 验证 key_template 自动编码 }}"
    class: VerifyKeyTemplateAutoFill
    params:
      pattern: "{sm_code}{SEQ:2}"
    priority: P0

# ==== 业务断言(非 DOM 断言) ====
questions:
  - ruleId: BR-business_object-DEL-condition
    description: "新建的业务对象应可通过 deletability.condition 校验"
    expected: true
    context:
      relationCount: 0
  - ruleId: BR-business_object-KEY
    description: "code 应自动填充为服务模块编码 + 2 位序号"
    expected:
      pattern: "{sm_code}{SEQ:2}"
    context:
      serviceModule: "SM01"
  - ruleId: BR-business_object-CS-domain_id
    description: "domain_id 下拉应只显示当前版本的 domain"
    expected: true
    context:
      versionId: "v1.0"

# ==== 测试数据 ====
data_tables:
  - name: business_objects
    rows:
      - code: "SM0101"
        name: "客户订单"
        # 业务字段
  - name: users
    rows:
      - username: "admin_user"
        role: "admin"

# ==== 清理策略 ====
cleanup:
  strategy: auto  # auto | manual | none
  tables:
    - business_objects
    - users

# ==== 元数据 ====
metadata:
  feature: "{{ 功能名,如 business-object-lifecycle }}"
  source_spec: ".trae/specs/<feat>/spec.md"
  source_rules: ".trae/specs/_business_rules/business_object.yaml"
  generated_at: "2026-06-13T10:00:00Z"
  model_name: "{{ 用户在 TRAE chat 选定的模型 }}"
