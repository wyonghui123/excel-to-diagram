# 分组启用状态优化 - 验收检查清单

## 阶段1：核心逻辑实现

### 1.1 叶子节点计数函数

- [x] `countLeafNodesInGroup` 函数已创建
- [x] 函数接受分组对象作为参数
- [x] 递归计算所有叶子节点数量
- [x] 返回正确的叶子节点总数

### 1.2 终止条件判断函数

- [x] `isLeafGroup` 函数已创建
- [x] `hasSpecialGroup` 函数已创建
- [x] `isInCenterScope` 函数已创建
- [x] 正确判断分组是否直接包含叶子节点
- [x] 正确判断是否包含有/无外部关系分组
- [x] 正确判断分组是否属于中心范围

### 1.3 分组启用状态优化函数

- [x] `optimizeGroupEnabledState` 函数已创建
- [x] 正确获取所有分组
- [x] 正确计算总节点数
- [x] 正确识别中心范围分组
- [x] 中心范围：从领域开始递归向下 disable
- [x] 中心范围终止条件：分组包含大于1个子分组
- [x] 中心范围终止条件：分组直接包含叶子节点
- [x] 非中心范围：按阈值判断是否 disable
- [x] 递归逻辑正确
- [x] 调用 `emitUpdate()` 触发更新

---

## 阶段2：UI 按钮实现

### 2.1 优化分组状态按钮

- [x] 按钮已添加到 LayoutSelector.vue
- [x] 按钮位置正确（在整体排序按钮旁边）
- [x] 点击事件绑定正确
- [x] 按钮样式一致

### 2.2 按钮状态控制

- [x] 有分组时按钮可用
- [x] 无分组时按钮禁用
- [x] 状态控制正确

---

## 阶段3：集成与导出

### 3.1 函数导出

- [x] `optimizeGroupEnabledState` 已在 defineExpose 中导出
- [x] LayoutSelector 可以访问该函数

### 3.2 调用链连接

- [x] LayoutSelector 正确获取 LayoutControlPanel 引用
- [x] 点击按钮时正确调用优化函数
- [x] 错误处理完善

---

## 阶段4：配置项实现

### 4.1 阈值配置项

- [ ] 配置项已添加到高级配置区域
- [ ] 默认值正确（20%）
- [ ] 输入范围限制正确（5%-50%）
- [ ] 配置项样式一致

### 4.2 配置传递

- [ ] 配置正确传递到 LayoutControlPanel
- [ ] 阈值在优化函数中正确使用

---

## 阶段5：验证效果

### 5.1 中心范围分组测试

- [ ] 中心范围的领域分组被设置为 disabled
- [ ] 子分组保持 enabled
- [ ] 布局更均衡

### 5.2 大分组优化测试

- [ ] 超过阈值的分组被设置为 disabled
- [ ] 子分组直接参与布局
- [ ] 布局更均衡

### 5.3 终止条件测试

- [ ] 直接包含叶子节点的分组不被处理
- [ ] 有/无外部关系分组不受影响
- [ ] 递归在正确位置停止

---

## 代码质量

- [x] 代码格式规范
- [x] 无语法错误
- [x] 函数命名清晰
- [x] 注释完整
- [x] 无性能问题

---

## 用户体验

- [x] 按钮位置合理
- [x] 按钮状态清晰
- [ ] 优化效果可见
- [ ] 操作流程顺畅
