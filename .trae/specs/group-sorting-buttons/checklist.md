# 分组排序按钮功能 - 验收检查清单

## 阶段1：排序逻辑实现

### 1.1 分组节点计数函数

- [x] `countNodesInGroup` 函数已创建
- [x] 函数接受分组对象作为参数
- [x] 递归计算所有节点数量
- [x] 返回正确的节点总数

### 1.2 分组连线密度计算函数

- [x] `calculateGroupConnectionDensity` 函数已创建
- [x] 函数接受分组和连线数组作为参数
- [x] 正确统计分组与其他分组的连线数量
- [x] 返回正确的连线密度

### 1.3 整体排序函数

- [x] `handleOverallSort` 函数已创建
- [x] 正确获取所有顶层分组
- [x] 按综合策略排序
- [x] 更新 `localConfig.value.groups`
- [x] 调用 `emitUpdate()` 触发更新

### 1.4 层内排序函数

- [x] `handleInLayerSort` 函数已创建
- [x] 正确识别虚拟层（`_isVirtualLayer`）
- [x] 对每个虚拟层的 children 排序
- [x] 调用 `emitUpdate()` 触发更新

---

## 阶段2：UI 按钮实现

### 2.1 整体排序按钮

- [x] 按钮已添加到 LayoutSelector.vue
- [x] 按钮位置正确（在虚拟分层按钮旁边）
- [x] 点击事件绑定正确
- [x] 按钮样式一致

### 2.2 层内排序按钮

- [x] 按钮已添加到 LayoutSelector.vue
- [x] 按钮位置正确
- [x] 点击事件绑定正确
- [x] 按钮样式一致

### 2.3 按钮状态控制

- [x] 整体排序按钮状态控制正确
- [x] 无分组时整体排序禁用
- [x] 层内排序按钮状态控制正确
- [x] 无虚拟分层时层内排序禁用

---

## 阶段3：集成与导出

### 3.1 函数导出

- [x] `handleOverallSort` 已在 defineExpose 中导出
- [x] `handleInLayerSort` 已在 defineExpose 中导出
- [x] LayoutSelector 可以访问这些函数

### 3.2 调用链连接

- [x] LayoutSelector 正确获取 LayoutControlPanel 引用
- [x] 点击按钮时正确调用排序函数
- [x] 错误处理完善

---

## 阶段4：验证效果

### 4.1 整体排序效果

- [x] 整体排序按钮可点击
- [x] 点击后分组顺序改变
- [x] 布局更均匀
- [x] 无报错

### 4.2 层内排序效果

- [x] 层内排序按钮可点击
- [x] 点击后虚拟层内部分组顺序改变
- [x] 层内布局更均匀
- [x] 无报错

### 4.3 组合场景效果

- [x] 场景 A（自动分组 → 整体排序）正常
- [x] 场景 B（自动分组 → 虚拟分层 → 层内排序）正常
- [x] 场景 C（完整流程）正常

---

## 代码质量

- [x] 代码格式规范
- [x] 无语法错误
- [x] 函数命名清晰
- [x] 注释完整
- [x] 无性能问题

---

## 用户体验

- [x] 按钮位置合理
- [x] 按钮状态清晰
- [x] 排序效果可见
- [x] 操作流程顺畅
