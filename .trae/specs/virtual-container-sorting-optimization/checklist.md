# 虚拟容器排序优化 - 验收检查清单

## 阶段1：分组大小排序策略

### 1.1 分组大小排序函数实现

- [x] `sortVirtualContainersBySize` 函数已创建
- [x] 函数接受虚拟容器数组作为参数
- [x] 按节点数量降序排序
- [x] 返回排序后的虚拟容器数组

---

## 阶段2：连线密度排序策略

### 2.1 连线密度计算函数实现

- [x] `calculateContainerConnectionDensity` 函数已创建
- [x] 函数接受虚拟容器数组和连线数组作为参数
- [x] 正确统计容器内部连线数量
- [x] 正确统计容器之间连线数量
- [x] 返回连线密度 Map

### 2.2 连线密度排序函数实现

- [x] `sortVirtualContainersByConnection` 函数已创建
- [x] 使用贪心算法排序
- [x] 连线多的容器靠近在一起
- [x] 返回排序后的虚拟容器数组

---

## 阶段3：综合排序策略

### 3.1 综合得分计算函数实现

- [x] `calculateContainerScores` 函数已创建
- [x] 分组大小得分正确归一化
- [x] 连线密度得分正确归一化
- [x] 加权组合逻辑正确
- [x] 权重可配置

### 3.2 综合排序函数实现

- [x] `sortVirtualContainers` 函数已创建
- [x] 整合分组大小和连线密度排序
- [x] 提供配置开关
- [x] 支持 'size', 'connection', 'combined' 三种策略

---

## 阶段4：集成到现有代码

### 4.1 集成排序逻辑

- [x] `generateMermaidCode` 中调用排序函数
- [x] 在构建虚拟容器后立即排序
- [x] 不影响现有功能
- [x] 向后兼容

### 4.2 配置参数

- [x] `containerSortingStrategy` 配置参数已添加
- [x] 支持 'size', 'connection', 'combined' 三种策略
- [x] 默认使用 'combined'
- [x] 配置参数可通过 layoutControlConfig 传递

---

## 阶段5：验证效果

### 5.1 分组大小排序效果

- [ ] 分组大小排序策略生效
- [ ] 节点多的容器在前
- [ ] 布局有所改善

### 5.2 连线密度排序效果

- [ ] 连线密度排序策略生效
- [ ] 有连线的容器靠近在一起
- [ ] 连线交叉减少

### 5.3 综合排序效果

- [ ] 综合排序策略生效
- [ ] 布局均匀性改善
- [ ] 整体可读性提升

---

## 代码质量

- [x] 代码格式规范
- [x] 无语法错误
- [x] 函数命名清晰
- [x] 注释完整
- [x] 无性能问题

---

## 文档更新（如需要）

- [ ] 配置选项说明已更新
- [ ] 用户知道如何选择排序策略
