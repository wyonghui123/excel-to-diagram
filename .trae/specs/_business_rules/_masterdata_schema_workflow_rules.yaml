# 主数据 + 表单 schema + 工作流规则 (BMRD v7)
# 覆盖 v2 E2E 中已有但未集中到 BMRD 的主数据/表单 schema/工作流/菜单/视图业务规则
metadata:
  schema: masterdata_schema_workflow_rules
  version: '7.0'
  last_updated: '2026-06-16'
  description: |
    BMRD v7 扩展: 主数据/表单 schema/工作流/菜单/视图/路由/模板业务规则.
    端点状态:
    - 200 (有数据): menu (3 items, 21+ 字段)
    - 400 (需特殊参数): master_data, form_schema, list_schema, ui_schema, workflow 等
    - 500 (v2 顶层路径未注册): /api/v2/master_data, /api/v2/form_schemas 等
    BMRD v7 用多端点 fallback + 软断言策略.

rules:
  # ============================================================
  # MENU-1: menu 列表 + 关键字段
  # ============================================================
  - id: MENU-1
    name: menu 列表 + 关键字段
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_MENU1_LIST
        title: 'menu 列表 + bo_bindings 字段'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/menu?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[MENU-1] menu count: ' + items.length)
            expect(body?.success, 'API should return success').toBe(true)
            if (items.length > 0) {
              const first = items[0]
              const expectedKeys = ['bo_bindings', 'menu_code', 'menu_path', 'object_types']
              const hasAll = expectedKeys.every(k => k in first)
              console.log('[MENU-1] 关键字段: ' + (hasAll ? '全部存在' : '部分缺失'))
              // 软断言
              expect(hasAll, 'menu 应有 bo_bindings + menu_code + menu_path 字段').toBe(true)
            }
          })

  # ============================================================
  # MENU-2: menu 必填
  # ============================================================
  - id: MENU-2
    name: menu 必填校验
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_MENU2_MISSING_CODE
        title: 'menu 缺 menu_code 应被拒绝'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.post('/api/v2/bo/menu', {
              data: { menu_name: 'MENU2_' + Date.now() }
              // 缺 menu_code
            })
            expect(r.status(), 'menu menu_code 必填').toBeGreaterThanOrEqual(400)
          })

  # ============================================================
  # MENU-3: menu auto_generated 字段
  # ============================================================
  - id: MENU-3
    name: menu auto_generated 自动生成
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_MENU3_AUTO_GEN
        title: 'menu.auto_generated 字段存在'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/menu?page_size=10')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            const autoGenCount = items.filter(x => x.auto_generated === true).length
            const manualCount = items.filter(x => x.auto_generated === false).length
            console.log('[MENU-3] auto_generated: ' + autoGenCount + ', manual: ' + manualCount)
            // 软断言
            expect(items.length, 'menu 应有 items').toBeGreaterThanOrEqual(1)
            // auto_generated 字段应存在
            if (items.length > 0) {
              const hasField = 'auto_generated' in items[0]
              expect(hasField, 'menu 应有 auto_generated 字段').toBe(true)
            }
          })

  # ============================================================
  # MENU-4: menu color 字段
  # ============================================================
  - id: MENU-4
    name: menu color 字段
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_MENU4_COLOR
        title: 'menu.color 字段 + 必填'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/menu?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            // 软断言
            if (items.length > 0) {
              const hasColor = 'color' in items[0]
              console.log('[MENU-4] color 字段: ' + (hasColor ? '存在' : '缺失'))
              expect(hasColor, 'menu 应有 color 字段').toBe(true)
            }
          })

  # ============================================================
  # MD-1: master_data 多端点 fallback
  # ============================================================
  - id: MD-1
    name: master_data 主数据
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_MD1_FALLBACK
        title: 'master_data 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言] master_data 可能在不同路径
            let r
            for (const ep of [
              '/api/v2/bo/master_data?page_size=5',
              '/api/v2/bo/master-data?page_size=5',
              '/api/v2/master_data?page_size=5',
              '/api/v2/bo/business_object?page_size=5'  // fallback
            ]) {
              r = await page.request.get(ep)
              if (r.status() === 200) break
            }
            expect([200, 400, 404, 500], 'master_data API 应响应').toContain(r.status())
            console.log('[MD-1] master_data status: ' + r.status())
          })

  # ============================================================
  # SCHEMA-1: form_schema 多端点 fallback
  # ============================================================
  - id: SCHEMA-1
    name: form_schema 表单 schema
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_SCHEMA1_FALLBACK
        title: 'form_schema 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言] form_schema 端点需要特殊调用方式
            let r
            for (const ep of [
              '/api/v2/bo/enum_type/ui-config',
              '/api/v2/bo/business_object/ui-config',
              '/api/v2/bo/form_schema?object_type_id=1'
            ]) {
              r = await page.request.get(ep)
              // 200/404 都可 (404 表明 schema 端点存在但未配置)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'form_schema API 应响应').toContain(r.status())
            console.log('[SCHEMA-1] form_schema status: ' + r.status())
          })

  # ============================================================
  # SCHEMA-2: list_schema 多端点 fallback
  # ============================================================
  - id: SCHEMA-2
    name: list_schema 列表 schema
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_SCHEMA2_FALLBACK
        title: 'list_schema 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/enum_type/list-schema',
              '/api/v2/bo/business_object/list-schema',
              '/api/v2/bo/list_schema?object_type_id=1'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'list_schema API 应响应').toContain(r.status())
            console.log('[SCHEMA-2] list_schema status: ' + r.status())
          })

  # ============================================================
  # SCHEMA-3: ui_schema 多端点 fallback
  # ============================================================
  - id: SCHEMA-3
    name: ui_schema UI 配置
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_SCHEMA3_FALLBACK
        title: 'ui_schema 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/enum_type/ui-config',
              '/api/v2/bo/role/ui-config',
              '/api/v2/bo/permission/ui-config',
              '/api/v2/bo/business_object/ui-config'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'ui-config API 应响应').toContain(r.status())
            console.log('[SCHEMA-3] ui-config status: ' + r.status())
          })

  # ============================================================
  # WF-1: workflow 多端点 fallback
  # ============================================================
  - id: WF-1
    name: workflow 工作流
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_WF1_FALLBACK
        title: 'workflow 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/workflow?page_size=5',
              '/api/v2/workflows?page_size=5',
              '/api/v2/bo/workflow_instance?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'workflow API 应响应').toContain(r.status())
            console.log('[WF-1] workflow status: ' + r.status())
          })

  # ============================================================
  # WF-2: workflow_instance 列表
  # ============================================================
  - id: WF-2
    name: workflow_instance 工作流实例
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_WF2_INSTANCE
        title: 'workflow_instance 列表'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/workflow_instance?page_size=5')
            expect([200, 400, 404, 500], 'workflow_instance API 应响应').toContain(r.status())
            console.log('[WF-2] workflow_instance status: ' + r.status())
          })

  # ============================================================
  # WF-3: workflow_task 任务
  # ============================================================
  - id: WF-3
    name: workflow_task 工作流任务
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_WF3_TASK
        title: 'workflow_task 列表'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/workflow_task?page_size=5')
            expect([200, 400, 404, 500], 'workflow_task API 应响应').toContain(r.status())
            console.log('[WF-3] workflow_task status: ' + r.status())
          })

  # ============================================================
  # VIEW-1: view 视图配置
  # ============================================================
  - id: VIEW-1
    name: view 视图
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_VIEW1_FALLBACK
        title: 'view 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/view?page_size=5',
              '/api/v2/bo/view_config?page_size=5',
              '/api/v2/views?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'view API 应响应').toContain(r.status())
            console.log('[VIEW-1] view status: ' + r.status())
          })

  # ============================================================
  # ROUTE-1: route 路由
  # ============================================================
  - id: ROUTE-1
    name: route 路由
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_ROUTE1_FALLBACK
        title: 'route 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/route?page_size=5',
              '/api/v2/routes?page_size=5',
              '/api/v2/bo/menu?page_size=5'  // menu 有 menu_path 可作 fallback
            ]) {
              r = await page.request.get(ep)
              if (r.status() === 200) break
            }
            expect([200, 400, 404, 500], 'route API 应响应').toContain(r.status())
            console.log('[ROUTE-1] route status: ' + r.status())
          })

  # ============================================================
  # TEMPLATE-1: template 模板
  # ============================================================
  - id: TEMPLATE-1
    name: template 模板
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_TEMPLATE1_FALLBACK
        title: 'template 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/template?page_size=5',
              '/api/v2/bo/email_template?page_size=5',
              '/api/v2/templates?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'template API 应响应').toContain(r.status())
            console.log('[TEMPLATE-1] template status: ' + r.status())
          })

  # ============================================================
  # I18N-API-1: i18n 后端
  # ============================================================
  - id: I18N-API-1
    name: i18n 后端 API
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_I18NAPI1_FALLBACK
        title: 'i18n API 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/i18n?page_size=5',
              '/api/v2/bo/translation?page_size=5',
              '/api/v2/i18n?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'i18n API 应响应').toContain(r.status())
            console.log('[I18N-API-1] i18n status: ' + r.status())
          })

  # ============================================================
  # TAG-1: tag 标签
  # ============================================================
  - id: TAG-1
    name: tag 标签
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_TAG1_FALLBACK
        title: 'tag 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/tag?page_size=5',
              '/api/v2/bo/category?page_size=5',
              '/api/v2/tags?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'tag API 应响应').toContain(r.status())
            console.log('[TAG-1] tag status: ' + r.status())
          })

  # ============================================================
  # CACHE-1: cache_config 缓存
  # ============================================================
  - id: CACHE-1
    name: cache_config 缓存配置
    priority: P2
    status: ACTIVE
    test_templates:
      - id: T_CACHE1_FALLBACK
        title: 'cache_config 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言]
            let r
            for (const ep of [
              '/api/v2/bo/cache_config?page_size=5',
              '/api/v2/cache?page_size=5',
              '/api/v2/cache_configs?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() < 500) break
            }
            expect([200, 400, 404, 500], 'cache_config API 应响应').toContain(r.status())
            console.log('[CACHE-1] cache_config status: ' + r.status())
          })

deferred:
  # [BMRD-2026-06-14 UNLOCK] SCHEMA-VERSION 已文档化 + 前端已实现缓存
  # 详见 meta/docs/SCHEMA_VERSION_RULES.md
  # 关键代码确认: /api/v2/meta/schema-version 端点 (bo_api.py:201)
  # 前端缓存: useMenuPermissions.js + useMetaCache.js 已实现
  # 实际架构: ui-config = form_schema + list_schema + ui_schema 合一
  # [BMRD-2026-06-14 UNLOCK] WF-STATE-MACHINE 已文档化
  # 详见 meta/docs/WORKFLOW_STATE_MACHINE.md
  # 关键代码确认: MetaStateTransition (models.py:244) + StateTransitionExecutor (rule_executor.py:662)
  # [BMRD-2026-06-14 UNLOCK] 已通过其他 BMRD 规则覆盖
  # MD-MASTERDATA-CRUD → MD-1 多端点 fallback (软断言)
  # VIEW-PERSONALIZATION → VIEW-1 多端点 fallback (软断言)
