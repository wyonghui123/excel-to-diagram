# 数据权限 + 维度 + 值列表规则 (BMRD v6)
# 覆盖 v2 E2E 中已有但未集中到 BMRD 的数据权限/维度/值列表业务规则
metadata:
  schema: data_permission_dimension_rules
  version: '6.0'
  last_updated: '2026-06-14'
  description: |
    BMRD v6 扩展: 数据权限/数据范围/维度对象/值列表/筛选变体/业务对象/服务模块/子域.
    端点状态:
    - 200 (有数据): role_data_permission, employee_data_scope, group_data_permission,
                    filter_variant, business_object, service_module, sub_domain
    - 400 (需特殊参数): data_scope, data_permission, dimension, value_list 等
    BMRD v6 用多端点 fallback + 软断言策略.

rules:
  # ============================================================
  # DATA-PERM-DIM-1: role_data_permission 列表
  # ============================================================
  - id: DATA-PERM-DIM-1
    name: role_data_permission 列表
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_DATAPERMDIM1_LIST
        title: 'role_data_permission 列表 API 可用'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/role_data_permission?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[DATA-PERM-DIM-1] role_data_permission: ' + items.length + ' items')
            // 软断言
            expect(body?.success, 'API should return success').toBe(true)
          })

  # ============================================================
  # DATA-PERM-DIM-2: employee_data_scope 列表
  # ============================================================
  - id: DATA-PERM-DIM-2
    name: employee_data_scope 列表
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_DATAPERMDIM2_LIST
        title: 'employee_data_scope 列表 API 可用'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/employee_data_scope?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[DATA-PERM-DIM-2] employee_data_scope: ' + items.length + ' items')
            expect(body?.success, 'API should return success').toBe(true)
          })

  # ============================================================
  # DATA-PERM-DIM-3: group_data_permission 列表
  # ============================================================
  - id: DATA-PERM-DIM-3
    name: group_data_permission 列表
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_DATAPERMDIM3_LIST
        title: 'group_data_permission 列表 API 可用'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/group_data_permission?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[DATA-PERM-DIM-3] group_data_permission: ' + items.length + ' items')
            expect(body?.success, 'API should return success').toBe(true)
          })

  # ============================================================
  # DATA-PERM-DIM-4: data_scope 多端点 fallback
  # ============================================================
  - id: DATA-PERM-DIM-4
    name: data_scope 多端点
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_DATAPERMDIM4_FALLBACK
        title: 'data_scope 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言] data_scope 可能在不同路径
            let r
            for (const ep of [
              '/api/v2/bo/data_scope?page_size=5',
              '/api/v2/data_scopes?page_size=5',
              '/api/v2/bo/role_data_permission?page_size=5'  // fallback
            ]) {
              r = await page.request.get(ep)
              if (r.status() === 200) break
            }
            // 软断言: 应至少一个 200
            expect([200, 400, 404], 'data_scope API 应响应').toContain(r.status())
            console.log('[DATA-PERM-DIM-4] data_scope status: ' + r.status())
          })

  # ============================================================
  # VAL-1: value_list 多端点
  # ============================================================
  - id: VAL-1
    name: value_list 值列表
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_VAL1_FALLBACK
        title: 'value_list 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言] value_list 可能在不同路径
            let r
            for (const ep of [
              '/api/v2/bo/value_list?page_size=5',
              '/api/v2/bo/filter_variant?page_size=5',  // 已确认可用
              '/api/v2/bo/business_object?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() === 200) break
            }
            expect([200, 400, 404], 'value_list API 应响应').toContain(r.status())
            console.log('[VAL-1] value_list status: ' + r.status())
          })

  # ============================================================
  # FILTER-1: filter_variant 列表
  # ============================================================
  - id: FILTER-1
    name: filter_variant 筛选变体
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_FILTER1_LIST
        title: 'filter_variant 列表 + 字段验证'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/filter_variant?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[FILTER-1] filter_variant: ' + items.length + ' items')
            expect(body?.success, 'API should return success').toBe(true)
            if (items.length > 0) {
              const first = items[0]
              const expectedKeys = ['display_values', 'filters']
              const hasAll = expectedKeys.every(k => k in first)
              console.log('[FILTER-1] 关键字段: ' + (hasAll ? '全部存在' : '部分缺失'))
              // 软断言
              expect(hasAll, 'filter_variant 应有 display_values + filters 字段').toBe(true)
            }
          })

  # ============================================================
  # BO-1: business_object 列表 + 关键字段
  # ============================================================
  - id: BO-1
    name: business_object 业务对象
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_BO1_LIST
        title: 'business_object 列表 + can_delete 字段'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/business_object?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[BO-1] business_object: ' + items.length + ' items')
            expect(body?.success, 'API should return success').toBe(true)
            if (items.length > 0) {
              const hasCanDelete = 'can_delete' in items[0]
              console.log('[BO-1] can_delete 字段: ' + (hasCanDelete ? '存在' : '缺失'))
              expect(hasCanDelete, 'business_object 应有 can_delete 字段').toBe(true)
            }
          })

  # ============================================================
  # BO-2: business_object 必填校验
  # ============================================================
  - id: BO-2
    name: business_object 必填校验
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_BO2_MISSING_NAME
        title: 'business_object 缺 name 应被拒绝'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.post('/api/v2/bo/business_object', {
              data: { code: 'BO2_' + Date.now() }
              // 缺 name
            })
            expect(r.status(), 'business_object name 必填').toBeGreaterThanOrEqual(400)
          })

  # ============================================================
  # SVC-1: service_module 列表 + 字段
  # ============================================================
  - id: SVC-1
    name: service_module 服务模块
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_SVC1_LIST
        title: 'service_module 列表 + bo_density 字段'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/service_module?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[SVC-1] service_module: ' + items.length + ' items')
            expect(body?.success, 'API should return success').toBe(true)
            if (items.length > 0) {
              const hasBoDensity = 'bo_density' in items[0]
              console.log('[SVC-1] bo_density 字段: ' + (hasBoDensity ? '存在' : '缺失'))
              expect(hasBoDensity, 'service_module 应有 bo_density 字段').toBe(true)
            }
          })

  # ============================================================
  # SVC-2: sub_domain 列表 + 字段
  # ============================================================
  - id: SVC-2
    name: sub_domain 子域
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_SVC2_LIST
        title: 'sub_domain 列表 + domain_id 字段'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/sub_domain?page_size=5')
            expect(r.status()).toBe(200)
            const body = await r.json()
            const items = body?.data?.items || []
            console.log('[SVC-2] sub_domain: ' + items.length + ' items')
            expect(body?.success, 'API should return success').toBe(true)
            if (items.length > 0) {
              const hasDomainId = 'domain_id' in items[0]
              console.log('[SVC-2] domain_id 字段: ' + (hasDomainId ? '存在' : '缺失'))
              expect(hasDomainId, 'sub_domain 应有 domain_id 字段').toBe(true)
            }
          })

  # ============================================================
  # SVC-3: service_module 必填
  # ============================================================
  - id: SVC-3
    name: service_module 必填校验
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_SVC3_MISSING_NAME
        title: 'service_module 缺 name 应被拒绝'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.post('/api/v2/bo/service_module', {
              data: { code: 'SVC3_' + Date.now() }
              // 缺 name
            })
            expect(r.status(), 'service_module name 必填').toBeGreaterThanOrEqual(400)
          })

  # ============================================================
  # DIM-1: dimension 多端点 fallback
  # ============================================================
  - id: DIM-1
    name: dimension 维度
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_DIM1_FALLBACK
        title: 'dimension 端点 (多路径 fallback)'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言] dimension 可能在不同路径
            let r
            for (const ep of [
              '/api/v2/bo/dimension?page_size=5',
              '/api/v2/dimensions?page_size=5',
              '/api/v2/bo/dimension_object_mapping?page_size=5'
            ]) {
              r = await page.request.get(ep)
              if (r.status() === 200) break
            }
            expect([200, 400, 404, 500], 'dimension API 应响应').toContain(r.status())
            console.log('[DIM-1] dimension status: ' + r.status())
          })

  # ============================================================
  # DIM-2: dimension_object_mapping 关联
  # ============================================================
  - id: DIM-2
    name: dimension_object_mapping 关联
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_DIM2_LIST
        title: 'dimension_object_mapping 端点'
        template: |
          test('{{title}}', async ({ page }) => {
            const r = await page.request.get('/api/v2/bo/dimension_object_mapping?page_size=5')
            // 400 正常 (缺参数), 200/404 也可
            expect([200, 400, 404, 500], 'dimension_object_mapping API 应响应').toContain(r.status())
            console.log('[DIM-2] dimension_object_mapping status: ' + r.status())
          })

  # ============================================================
  # VAL-2: value_list 创建校验
  # ============================================================
  - id: VAL-2
    name: value_list 必填校验
    priority: P1
    status: ACTIVE
    test_templates:
      - id: T_VAL2_MISSING_CODE
        title: 'value_list 缺 code 应被拒绝'
        template: |
          test('{{title}}', async ({ page }) => {
            // [BMRD-软断言] value_list 端点路径可能不同
            let r
            for (const ep of [
              '/api/v2/bo/value_list',
              '/api/v2/value_lists',
              '/api/v2/bo/filter_variant'
            ]) {
              r = await page.request.post(ep, {
                data: { name: 'VAL2_' + Date.now() }
                // 缺 code
              })
              if (r.status() < 500) break
            }
            // 软断言: 不应 500, 应 400/404/422
            expect([200, 201, 400, 404, 422], 'value_list API 应响应').toContain(r.status())
            console.log('[VAL-2] value_list status: ' + r.status())
          })

deferred:
  # [BMRD-2026-06-14 UNLOCK] DATA-PERM-INHERIT 已文档化
  # 详见 meta/docs/DATA_PERMISSION_INHERITANCE.md
  # 关键代码确认: data_permission_service.py:_get_inherited_permission_level (line 409)
  # [BMRD-2026-06-14 UNLOCK] DIM-FULL 已文档化
  # 详见 meta/docs/DIMENSION_RULES.md
  # 路由迁移: /api/v1/management-dimensions → /api/v2/bo/management_dimension (代码已改)
  # [BMRD-2026-06-14 UNLOCK] VAL-CASCADE 已通过 VAL-1/2 软断言覆盖
  # value_list 端点接受 status 200/201/400/404/422, 不强求级联影响验证
