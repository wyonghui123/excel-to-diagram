# 打包部署交接文档 V20260712_001

> 生成时间: 2026-07-12 | 源分支: release-prep-worktree | 基线 commit: 9ec9c69 (HEAD)
>
> 变更历史:
> - V20260710_001 (2026-07-10): 初始版本, 基线 b6198d8
> - V20260712_001 (2026-07-12): 补充 c333ba0/9ec9c69 前端改动 + core_service/observability_service 元能力基础设施

---

## 一、版本概述

本版本包含 **8 个 BUG 修复 + 3 个功能增强 + 元能力基础设施 + 部署工具链强化**，涵盖数据权限、owner chain、schema 暴露、审计日志权限、关系唯一性、导入策略、Mermaid 渲染/导出、元能力解耦等核心改动。

### BUG 修复清单

| BUG ID | 标题 | 严重度 | 涉及文件数 | Commit |
|--------|------|--------|-----------|--------|
| BUG-V048 | 对象范围 chip 双重计数 (141→282) | P1 | 2 | a5ede6e 等 |
| BUG-V049 | version 唯一索引阻塞跨产品同名版本 | P1 | 3 | 837ec12 |
| BUG-V050 | owner chain 覆盖范围不足 (SM/BO/relationship) | P0 | 7 | a5ede6e |
| BUG-V051 | schema 字段暴露不足 + import/export actions 缺失 | P1 | 10 | a5ede6e |
| BUG-V052 | 审计日志权限 code 双重前缀 | P1 | 3 | a5ede6e |
| BUG-V053 | 关系唯一性约束逻辑错误 + 导入 upsert 路径 | P1 | 3 | a5ede6e |
| BUG-V054 | 导入权限检查绕过 owner chain 豁免 | P1 | 1 | e6a2108 |
| V007.58-61 | mermaid 11.13 BO 图 syntax error 真根因 | P0 | 8 | b6198d8 |

### 功能增强

| ID | 标题 | 涉及文件数 | Commit |
|----|------|-----------|--------|
| V007.49 | BO 图关系数量告警 (>100 触发 ElNotification) | 2 | 0c9dbbb |
| V007.62 | dompurify+jsdom 安全过滤 + MermaidComponent 大重构 | 3 | c333ba0 |
| V007.63 | mermaid 导出优化 + UI 调整 (PDF→JPEG/SVG修复/StepDisplay) | 10 | 9ec9c69 |

### 元能力基础设施 (新增)

| 服务 | 版本 | 端口 | 说明 | Commit |
|------|------|------|------|--------|
| core_service | v2.0 | 9200 (HTTPS) | 核心元能力: upload + exec + audit (冻结) | 150c37b |
| observability_service | v1.0 | 9201 (HTTP) | 可观测性扩展: health + metrics + upload_multi + request_id | 150c37b |
| log_service | v4.10 | 9101 | 日志服务 + 备份 exec 通道 | 17c4bb6 |

---

## 二、核心文件清单（必须包含在 zip 中）

### 2.1 后端源文件（未提交，需打包）

以下文件有实际代码修改，**必须**包含在部署包中：

| # | 文件路径 | 变更主题 | 改动行数 |
|---|---------|---------|---------|
| 1 | `meta/api/audit_api.py` | BUG-V052: 统一权限 code 短名 + 新增 write/export helper | +46 |
| 2 | `meta/api/bo_api.py` | BUG-V050: _check_single_bo_in_dim_scope 增加 owner chain + data_permissions 兜底 | +34 |
| 3 | `meta/core/bo_framework.py` | BUG-V051.2: get_schema 暴露 15+ 新字段 | +30 |
| 4 | `meta/core/interceptors/data_permission_interceptor.py` | BUG-V050: relationship dim scope 叠加 allowed_ids 兜底 + BO 扩展 | +82 |
| 5 | `meta/core/interceptors/owner_chain_interceptor.py` | BUG-V050: relationship _PARENT_FIELD + BUG-V052 user_info fix + 多字段名兼容 | +46 |
| 6 | `meta/core/interceptors/write_scope_interceptor.py` | BUG-V050: _ASSOCIATE_DST_KEY fix + _is_fk_value_in_scope 3层权限检查 | +67 |
| 7 | `meta/core/metadata_driven_validator.py` | BUG-V053: is_pure_bk_index 区分 + 空字符串跳过 | +24 |
| 8 | `meta/schemas/audit_log.yaml` | BUG-V052: action.id 改标准短名 + 新增 export | +20 |
| 9 | `meta/schemas/business_object.yaml` | BUG-V051: batch_import→import + export 路径统一 | +14 |
| 10 | `meta/schemas/domain.yaml` | BUG-V051: 补全 import/export actions | +14 |
| 11 | `meta/schemas/product.yaml` | BUG-V051: 补全 import/export actions | +15 |
| 12 | `meta/schemas/relationship.yaml` | BUG-V051+V053: import/export + 索引变更(唯一→复合+code唯一) | +40 |
| 13 | `meta/schemas/schema_loader.py` | BUG-V051.2: 大幅扩展序列化 (26+ 字段) | +200 |
| 14 | `meta/schemas/service_module.yaml` | BUG-V051: 补全 import/export actions | +14 |
| 15 | `meta/schemas/sub_domain.yaml` | BUG-V051: 补全 import/export actions | +14 |
| 16 | `meta/schemas/version.yaml` | BUG-V051: 补全 import/export actions | +15 |
| 17 | `meta/scripts/init_menu_permissions.py` | BUG-V051: 补全所有架构对象 import/export 权限 | +16 |
| 18 | `meta/services/chain_owner_resolver.py` | BUG-V050: 移除 is_in_chain 门禁 + SM/BO/relationship SQL | +155 |
| 19 | `meta/services/import_export_service.py` | BUG-V050+V053+V054: owner 改 build_owner_exception_subquery + 重复降级 warning + upsert 路径修 + 导入权限 owner chain 豁免 | +310 |
| 20 | `meta/services/query_service.py` | BUG-V050: owner exception 从 is_in_chain 改 build_owner_exception_subquery + OR 合并 | +85 |
| 21 | `meta/core/db_config_detector.py` | V007.50: db_path 字段供 /health 端点 | +6 |
| 22 | `meta/core/sql_connection_pool.py` | V007.50: 模块级 _health_check/_set_pool 供 server 导入 | +47 |

### 2.2 后端源文件（已提交，已在 zip 中但需更新）

| # | 文件路径 | Commit | 变更主题 |
|---|---------|--------|---------|
| 23 | `meta/core/safe_connect.py` | 62c063a | V007.46: disk I/O 重试 + 连接重建 |
| 24 | `meta/core/sql_connection_pool.py` | 62c063a | V007.46: mmap_size=64MB + PASSIVE checkpoint |
| 25 | `meta/core/diagnostics.py` | 62c063a | V007.46: 连接诊断增强 |
| 26 | `meta/server.py` | 61cbfd4 | V007.47: /health 端点 + PRAGMA 幂等 |
| 27 | `meta/migrations/fix_version_unique_index.py` | 837ec12 | BUG-V049: version 唯一索引迁移 |

### 2.3 前端源文件（需重新构建）

| # | 文件路径 | 变更主题 | Commit |
|---|---------|---------|--------|
| 28 | `src/components/common/ImportDialog/ImportDialog.vue` | 冲突策略 update_only→skip + UI 标签修正 | a5ede6e |
| 29 | `src/components/MermaidComponent.vue` | V007.62: dompurify 安全过滤 + 大重构 (698+/272-) | c333ba0 |
| 30 | `src/composables/useMermaid/annotation/annotationOverlay.js` | V007.63: annotation 覆盖层调整 | 9ec9c69 |
| 31 | `src/composables/useMermaid/interaction/useInteraction.js` | V007.63: 交互逻辑调整 | 9ec9c69 |
| 32 | `src/composables/useMermaid/renderer/useSvgProcessor.js` | V007.63: SVG 处理器调整 | 9ec9c69 |
| 33 | `src/composables/useMermaid/style/useSvgStyle.js` | V007.63: SVG 样式调整 | 9ec9c69 |
| 34 | `src/composables/useMermaid/tooltip/useTooltip.js` | V007.63: tooltip 调整 | 9ec9c69 |
| 35 | `src/composables/useMermaid/config/useMermaidConfig.js` | V007.58-61: maxEdges top-level 配置 | b6198d8 |
| 36 | `src/composables/useMermaid/syntax/_shared/arrowHelper.js` | V007.58: HTML entity → #XX; 原生转义 | b6198d8 |
| 37 | `src/views/AADiagramApp/components/StepNavigator.vue` | V007.63: 类型标签浅灰小标签 | 9ec9c69 |
| 38 | `src/views/AADiagramApp/components/steps/StepDisplay.vue` | V007.63: 顶部横条删除 | 9ec9c69 |
| 39 | `src/views/AADiagramApp/index.vue` | V007.63: 样式调整 | 9ec9c69 |
| 40 | `vite.config.js` | 代理 target 从 3018 改回 3011 (仅影响 dev server, 不影响构建产物) | - |

以下文件 `git diff HEAD` 无实际内容变更（仅 CRLF/LF 差异），无需额外处理：
- `src/components/common/RelationScopeTree/RelationScopeTree.vue`
- `src/composables/useMermaid/layouts/gridLayout.js`
- `src/composables/useMermaid/layouts/groupedLayout.js`
- `src/services/groupModel/MermaidGenerator.js`
- `src/services/serviceModuleDiagramBuilder.js`

### 2.4 元能力基础设施文件（新增，需独立部署）

| # | 文件路径 | 说明 | Commit |
|---|---------|--------|--------|
| 41 | `tools/core_service.py` | core_service v2.0 核心元能力 (483 行, HTTPS 9200) | 150c37b |
| 42 | `tools/observability_service.py` | observability_service v1.0 可观测性扩展 (350 行, HTTP 9201) | 150c37b |
| 43 | `tools/log_service.py` | log_service v4.10 日志+备份exec (9101) | 17c4bb6 |
| 44 | `tools/e2e_core_service.py` | 双服务端到端测试 (72 case) | 150c37b |

**元能力部署架构**:
```
yonaa (172.20.59.7)
  ├── core_service.py   9200 HTTPS  [冻结] upload/exec/audit
  ├── observability_service.py  9201 HTTP  [独立重启] health/metrics/upload_multi/request_id
  └── log_service.py    9101 HTTP  日志+备份exec通道
```

---

## 三、数据库迁移（部署后必须执行）

### 3.1 BUG-V049: version 唯一索引迁移

```bash
python meta/migrations/fix_version_unique_index.py
```

- 删除旧索引 `uidx_versions_name` (name 单字段全局唯一)
- 创建新索引 `uidx_versions_product_name` (product_id, name 联合唯一)
- **幂等**: 重复执行不会报错

### 3.2 BUG-V053: relationship 索引变更

**注意: 此迁移没有独立脚本, 由 YAML + index_rule_engine 自动处理**

- 旧索引 `uidx_relationships_version_source_target_type` (version_id, source_bo_id, target_bo_id, relation_code) UNIQUE → 删除
- 旧索引 `uidx_relationships_version_source_target_direction` (version_id, source_bo_id, target_bo_id, relation_direction) UNIQUE → 删除
- 新索引 `idx_relationships_version_source_target_type_dir` (version_id, source_bo_id, target_bo_id, relation_type, relation_direction) COMPOSITE (非唯一)
- 新索引 `uidx_relationships_version_code` (version_id, code) UNIQUE (应用层校验)

**建议**: 部署前手动执行 SQL 确认索引状态:

```sql
-- 检查现有索引
SELECT name, sql FROM sqlite_master WHERE type='index' AND tbl_name='relationships';

-- 如果旧唯一索引存在, 需要删除并重建:
DROP INDEX IF EXISTS uidx_relationships_version_source_target_type;
DROP INDEX IF EXISTS uidx_relationships_version_source_target_direction;
CREATE INDEX IF NOT EXISTS idx_relationships_version_source_target_type_dir ON relationships(version_id, source_bo_id, target_bo_id, relation_type, relation_direction);
-- uidx_relationships_version_code 由应用层校验, 不加 DB unique constraint
```

### 3.3 BUG-V052: 审计日志权限 code 重命名

**需执行 init_menu_permissions.py 更新权限数据:**

```bash
python meta/scripts/init_menu_permissions.py
```

**DB 层面需手动执行 (如果已存在旧权限 code):**

```sql
-- 重命名权限 code (消除双重前缀)
UPDATE role_permissions SET permission_code = 'audit_log:list' WHERE permission_code = 'audit_log:audit_log_list';
UPDATE role_permissions SET permission_code = 'audit_log:read' WHERE permission_code = 'audit_log:audit_log_read';
UPDATE role_permissions SET permission_code = 'audit_log:delete' WHERE permission_code = 'audit_log:audit_log_delete';

-- 清理无法映射的旧权限记录
DELETE FROM role_permissions WHERE permission_code LIKE 'audit_log:audit_log_%';
```

### 3.4 元能力基础设施部署 (新增)

**3.4.1 自签 TLS 证书 (core_service HTTPS 9200 需要)**

```bash
# 在 yonaa 上执行一次 (已存在的可跳过)
sudo mkdir -p /opt/app/shared
sudo openssl req -x509 -newkey rsa:2048 -keyout /opt/app/shared/core_service.key \
  -out /opt/app/shared/core_service.crt -days 365 -nodes \
  -subj "/CN=172.20.59.7"
```

**3.4.2 部署 core_service + observability_service**

```bash
# 上传到 yonaa
scp tools/core_service.py yonaa:/opt/app/shared/
scp tools/observability_service.py yonaa:/opt/app/shared/
scp tools/log_service.py yonaa:/opt/app/shared/

# 设置环境变量 (在 systemd unit 或 .env 中)
export CORE_SERVICE_ADMIN_SECRET="v007.52-core-admin"
export CORE_SERVICE_WRITE_SECRET="v007.52-core-write"
export CORE_SERVICE_READ_SECRET="v007.52-core-read"
export OBS_CORE_URL="https://127.0.0.1:9200"
export OBS_AUDIT_LOG="/var/log/core_service_audit.log"

# 启动 core_service (HTTPS 9200)
cd /opt/app/shared
/opt/miniconda3-py39/bin/python core_service.py &

# 启动 observability_service (HTTP 9201)
/opt/miniconda3-py39/bin/python observability_service.py &

# 启动 log_service (HTTP 9101)
/opt/miniconda3-py39/bin/python log_service.py &
```

**3.4.3 Systemd unit 配置 (推荐，确保开机自启)**

```ini
# /etc/systemd/system/core_service.service
[Unit]
Description=Core Meta-Capability Service (v2.0)
After=network.target

[Service]
Type=simple
ExecStart=/opt/miniconda3-py39/bin/python /opt/app/shared/core_service.py
Environment=CORE_SERVICE_ADMIN_SECRET=v007.52-core-admin
Environment=CORE_SERVICE_WRITE_SECRET=v007.52-core-write
Environment=CORE_SERVICE_READ_SECRET=v007.52-core-read
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```ini
# /etc/systemd/system/observability_service.service
[Unit]
Description=Observability Extension Service (v1.0)
After=core_service.service

[Service]
Type=simple
ExecStart=/opt/miniconda3-py39/bin/python /opt/app/shared/observability_service.py
Environment=OBS_CORE_URL=https://127.0.0.1:9200
Environment=OBS_AUDIT_LOG=/var/log/core_service_audit.log
Environment=CORE_SERVICE_ADMIN_SECRET=v007.52-core-admin
Environment=CORE_SERVICE_WRITE_SECRET=v007.52-core-write
Environment=CORE_SERVICE_READ_SECRET=v007.52-core-read
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

**3.4.4 E2E 验证**

```bash
# 在本地执行, 验证 yonaa 上的双服务
scp tools/e2e_core_service.py yonaa:/tmp/
ssh yonaa "cd /tmp && /opt/miniconda3-py39/bin/python e2e_core_service.py"
# 预期: 72 case PASS (46 core + 25 obs + 1 decoupling)
```

---

## 四、部署步骤（精确顺序）

### PHASE 0: 打包

```powershell
# 1. 先提交所有未提交的修改 (如有)
cd d:\filework\release-prep-worktree
git add meta/ src/components/ src/composables/ src/views/AADiagramApp/
git commit -m "fix: BUG-V050/V051/V052/V053 + V007.62/V007.63 frontend updates"

# 2. 重新构建前端 (必须! c333ba0 + 9ec9c69 改了 13+ 前端文件)
npm run build

# 3. 重新打包 deploy bundle
python tools/rebuild_bundle.ps1

# 4. 验证 bundle
python tools/verify_deploy_bundle.py   # 必须 6/6 PASS
python tools/run_all_deploy_tests.py   # 必须 ALL PASS
```

### PHASE 1: 部署主应用到 yonaa

```bash
# 使用 deploy.sh 完整流程
bash tools/deploy.sh --version v20260712_001 --port 3011

# 或使用 smart_deploy.sh
bash tools/smart_deploy.sh
```

### PHASE 2: 部署元能力基础设施 (新增)

```bash
# 2.1 上传核心服务文件
scp tools/core_service.py yonaa:/opt/app/shared/
scp tools/observability_service.py yonaa:/opt/app/shared/
scp tools/log_service.py yonaa:/opt/app/shared/

# 2.2 TLS 证书 (如尚未配置)
ssh yonaa "sudo mkdir -p /opt/app/shared && sudo openssl req -x509 -newkey rsa:2048 \
  -keyout /opt/app/shared/core_service.key -out /opt/app/shared/core_service.crt \
  -days 365 -nodes -subj '/CN=172.20.59.7'"

# 2.3 启动服务 (如已有旧版本, 先 pkill)
ssh yonaa "pkill -f core_service.py; pkill -f observability_service.py; sleep 1"
ssh yonaa "cd /opt/app/shared && CORE_SERVICE_ADMIN_SECRET=v007.52-core-admin \
  CORE_SERVICE_WRITE_SECRET=v007.52-core-write \
  CORE_SERVICE_READ_SECRET=v007.52-core-read \
  nohup /opt/miniconda3-py39/bin/python core_service.py > /var/log/core_service.log 2>&1 &"
ssh yonaa "cd /opt/app/shared && OBS_CORE_URL=https://127.0.0.1:9200 \
  OBS_AUDIT_LOG=/var/log/core_service_audit.log \
  CORE_SERVICE_ADMIN_SECRET=v007.52-core-admin \
  CORE_SERVICE_WRITE_SECRET=v007.52-core-write \
  CORE_SERVICE_READ_SECRET=v007.52-core-read \
  nohup /opt/miniconda3-py39/bin/python observability_service.py > /var/log/observability_service.log 2>&1 &"

# 2.4 E2E 验证
scp tools/e2e_core_service.py yonaa:/tmp/
ssh yonaa "cd /tmp && /opt/miniconda3-py39/bin/python e2e_core_service.py"
```

### PHASE 3: 部署后迁移 (必须!)

```bash
# 3.1 BUG-V049: version 唯一索引
python meta/migrations/fix_version_unique_index.py

# 3.2 BUG-V053: relationship 索引 (手动 SQL)
sqlite3 /path/to/architecture.db "
  DROP INDEX IF EXISTS uidx_relationships_version_source_target_type;
  DROP INDEX IF EXISTS uidx_relationships_version_source_target_direction;
  CREATE INDEX IF NOT EXISTS idx_relationships_version_source_target_type_dir ON relationships(version_id, source_bo_id, target_bo_id, relation_type, relation_direction);
"

# 3.3 BUG-V052: 审计日志权限 code 重命名
python meta/scripts/init_menu_permissions.py

# 3.4 BUG-V052: DB 层面权限 code 修正
sqlite3 /path/to/architecture.db "
  UPDATE role_permissions SET permission_code = 'audit_log:list' WHERE permission_code = 'audit_log:audit_log_list';
  UPDATE role_permissions SET permission_code = 'audit_log:read' WHERE permission_code = 'audit_log:audit_log_read';
  UPDATE role_permissions SET permission_code = 'audit_log:delete' WHERE permission_code = 'audit_log:audit_log_delete';
  DELETE FROM role_permissions WHERE permission_code LIKE 'audit_log:audit_log_%';
"
```

### PHASE 4: 验证

```bash
# 4.1 主应用健康检查
curl http://yonaa:3011/health

# 4.2 元能力服务健康检查
curl -k https://yonaa:9200/api          # core_service v2.0
curl http://yonaa:9201/api/health       # observability_service
curl http://yonaa:9201/api/ready        # readiness (检查 core 连通性)

# 4.3 E2E 双服务验证 (72 case)
ssh yonaa "cd /tmp && /opt/miniconda3-py39/bin/python e2e_core_service.py"

# 4.4 V8ab 业务回归测试
python tools/V8ab_concurrent_test.py

# 4.5 前端验证 (人工)
# - wyonghui 登录, 检查关系列表是否可见 (BUG-V050)
# - 检查对象详情页操作日志是否显示 (BUG-V051)
# - 检查不同产品是否可创建同名版本 V1 (BUG-V049)
# - 检查审计日志权限是否正常 (BUG-V052)
# - 导入重复关系应显示 warning 而非报错 (BUG-V053)
# - wyonghui 在自己拥有的 product 下导入含领域的 Excel 应成功 (BUG-V054, 已本地验证通过)
# - Mermaid 大图 (≥1500 边) 自动切换 ELK 布局, 不再 stack overflow (V007.58-61)
# - PDF 导出体积显著减小 (V007.63)
# - StepDisplay 顶部横条已删除, 类型标签在 StepNavigator 旁 (V007.63)
```

---

## 五、风险评估

### 5.1 高风险项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| relationship 索引变更 (唯一→复合) | 历史脏数据可能违反新约束 | 先 SELECT 查重, 确认无冲突再 DDL |
| 审计日志权限 code 重命名 | 旧 code 不再被识别, 已授权用户可能失去权限 | 迁移 SQL 必须执行; 建议先备份 role_permissions 表 |
| schema_loader 大幅扩展 | 前端可能收到之前没见过的字段 | 纯新增字段, 不删不改变有名, 向后兼容 |
| chain_owner_resolver 移除 is_in_chain | 权限范围可能扩大 | 新增的 SM/BO/relationship 追溯都是正确的 owner 路径 |
| MermaidComponent 大重构 (c333ba0) | 渲染管线变化, 可能影响特定图 | dompurify 只过滤不改变逻辑; E2E 验证 3299 关系场景 |
| core_service 首次部署 yonaa | 新增 3 个端口 (9200/9201/9101) | 防火墙无需开放 (仅内部调用); systemd 配置自启 |
| observability_service 依赖 core_service | core 挂了 obs /ready 失败 | 解耦设计: obs 挂了 core 不受影响; 反之 core 挂只影响 /ready |

### 5.2 回退方案

- 部署前备份 `architecture.db`
- 保留上一个 deploy zip
- smart_deploy.sh 支持自动回退

---

## 六、commit 清单（b6198d8 → 9ec9c69 HEAD）

### 6.1 b6198d8 之前的 commit (HANDOFF V20260710_001 原始覆盖)

以下 commit 已在 git 中但旧 zip 严重滞后 (V20260703_004):

| Commit | 内容 | 是否已包含在旧 zip |
|--------|------|:--:|
| 837ec12 | BUG-V049 version 唯一索引 | 否 |
| b6198d8 | V007.58-61 mermaid syntax error 真根因 | 否 |
| 9288b6d | BUG-V048 chip 双重计数 | 否 |
| 95c8197 | V007.49 BO 图关系数量告警 | 否 |
| 33ffc88 | V007.50/V007.51 告警时机前移 | 否 |
| c914217 | 2 部署端点 + rebuild zip fallback | 否 |
| 62c063a | V007.46 disk I/O 真根因 6 FIX | 否 |
| 61cbfd4 | V007.47 db-level PRAGMA 幂等保护 | 否 |
| 596486f | V007.45 audit_logs migration | 否 |

### 6.2 b6198d8 之后新增的 commit (V20260712_001 补充)

**HANDOFF V20260710_001 未覆盖, 本次补充:**

| Commit | 内容 | 分类 | 影响范围 |
|--------|------|------|---------|
| e6a2108 | BUG-V054 导入权限绕过 owner chain 豁免 | 后端 BUG | import_export_service.py |
| a5ede6e | BUG-V050/V051/V052/V053 综合修复 | 后端 BUG | 22 个后端文件 |
| 837ec12 | BUG-V049 version 唯一索引 | 后端 BUG | 迁移脚本 |
| 0c9dbbb | V007.49 WAL→DELETE + safe_connect retry + sqlean + maxEdges | 后端关键修复 | safe_connect.py, sql_connection_pool.py, server.py |
| e1ef9f5 | deploy toolchain log_service + smart_deploy 增强 | 部署工具链 | log_service.py, smart_deploy.sh |
| c333ba0 | dompurify+jsdom + MermaidComponent 大重构 | **前端核心** | MermaidComponent.vue (698+/272-) |
| 9ec9c69 | mermaid 导出优化 + UI 调整 | **前端增强** | 10 文件 (913+/178-) |

### 6.3 元能力基础设施 commit (新增)

| Commit | 内容 | 服务 |
|--------|------|------|
| 17c4bb6 | core_service v1.1 + log_service v4.10 | 元能力 v1.1 |
| b60644b | e2e_core_service.py 16 case 测试 | 测试 |
| f8cd710 | HANDOFF 元能力交接文档 | 文档 |
| f1f54d0 | core_service v1.2 audit log | 演进 |
| 9c0e024 | core_service v1.3 三级权限 | 演进 |
| 3452fd8 | core_service v1.4 HTTPS | 演进 |
| 0ac8188 | systemd unit + HANDOFF 更新 | 运维 |
| 1d8860e | core_service v1.5 audit 轮转 | 演进 |
| bc04403 | core_service v1.6 批量上传 | 演进 |
| 4461bcc | core_service v1.7 Prometheus 监控 | 演进 |
| a57a197 | core_service v1.8 健康检查 | 演进 |
| 779ab73 | core_service v1.9 请求 ID | 演进 |
| 150c37b | v2.0 解耦 — observability_service | **最终态** |

**结论**: 必须**重新打包** (rebuild_bundle.ps1) + **npm run build** (前端 13+ 文件改动), 不能使用旧 zip。

---

## 七、调试插桩清理建议

`useDiagramData.js` 中的 `[V007.55 DEBUG]` localStorage 写入是临时调试代码，建议在发布前移除或注释掉:

```javascript
// 建议移除这段代码 (约 8 行):
try {
  localStorage.setItem('__v00755_mermaidCode', unifiedMermaidCode)
  localStorage.setItem('__v00755_mermaidCode_len', String(unifiedMermaidCode.length))
  console.log('[V007.55 DEBUG] BO mermaidCode written to localStorage, len=' + unifiedMermaidCode.length)
} catch (e) {
  console.warn('[V007.55 DEBUG] localStorage write failed:', e?.message)
}
```

---

## 八、完整性校验

### 打包前必查

- [ ] 22 个后端源文件全部已提交
- [ ] 前端已重新 `npm run build` (c333ba0 + 9ec9c69 改了 13+ 前端文件, 必须 rebuild)
- [ ] `verify_deploy_bundle.py` 6/6 PASS
- [ ] `run_all_deploy_tests.py` ALL PASS
- [ ] `rebuild_bundle.ps1` 末尾 SHA256 验证通过

### 部署后必查 (主应用)

- [ ] `/health` 返回 200 且无 error 字段
- [ ] `fix_version_unique_index.py` 执行成功
- [ ] relationship 索引变更 SQL 执行成功
- [ ] `init_menu_permissions.py` 执行成功
- [ ] 审计日志权限 code 迁移 SQL 执行成功
- [ ] wyonghui 登录验证: 关系列表可见 + 操作日志可见

### 部署后必查 (元能力基础设施)

- [ ] `curl -k https://yonaa:9200/api` 返回 v2.0
- [ ] `curl http://yonaa:9201/api/health` 返回 200
- [ ] `curl http://yonaa:9201/api/ready` 返回 200 (core 连通性)
- [ ] `e2e_core_service.py` 72 case PASS
- [ ] 解耦验证: 停 observability 后 core_service 正常运行
