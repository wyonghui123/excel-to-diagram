# V007.86b_PERMANENT_FIX_PROGRESS — 计划任务重建进展 + 实际错误诊断

> **报告日期**: 2026-07-17 09:53
> **作者**: Dev Agent (V061 staging) / 部署智能体
> **背景**: V007.86 用户报告"远程监控没有了", V007.86b 用 elevated schtasks 重建计划任务
> **状态**: 任务**已重建** (但**有 bug, 跑失败**), daemon 临时方案恢复, 等待用户跑 v4 升级

---

## 1. V007.86b 关键发现 (3 个真相)

### 1.1 真相 1: 老 yonaa_alert_monitor.xml 本身是损坏的 (GBK 错乱)

**V007.79c** 我用 `latin1 -> UTF-8` 转 UTF-16 LE 文件时, 用了**错的 fallback**:
- 文件原 UTF-16 LE (`FF FE` BOM)
- 转换: `latin1` 读取 0xFF 0xFE 当作两个 latin1 字符, 存到 UTF-8 文件
- 结果: 文件**实际是 GBK 编码** + 内容错乱

**修复** (V007.86b): 用 Python 重新生成干净 UTF-16 LE XML (3968 字节, BOM FFFE, 路径正确)

### 1.2 真相 2: 老 schtasks 命令用 alert_monitor.bat (有 --check-now)

```
要运行的任务: D:\filework\release-prep-worktree\tools\alert_monitor.bat
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
              .bat 内部有 --check-now + --log-file
```

**V007.59 计划任务**用 alert_monitor.bat (V007.58 老版本, 跑 alert_monitor.py --check-now).
**V007.62 升级**用 alert_monitor_v0760.bat (跑 v0760.py --check-now, V007.60 分层检查).

### 1.3 真相 3: V007.86b v2 XML 直接调 pythonw.exe 没 --check-now (失败)

**V007.86b v2** 我直接调:
```
pythonw.exe "v0760.py" --config ... --log-file ...   ← 没 --check-now!
```

**alert_monitor_v0760.py main()**:
```python
if args.check_now:
    rc = run_once_v0760(...)
elif args.daemon:
    ...
else:
    parser.print_help()   ← 我的命令走这里!
    rc = 1
```

**结果**: 每次跑都 print_help + return 1, **写 log 失败, 任务失败**.

---

## 2. V007.86b 实际进度 (按时间)

### 2.1 9:23:00 - Stop daemon 8860 (我之前 V007.86 启动的)

```bash
Get-Process python | Stop-Process
```

✅ Daemon stopped, 没 python 进程在跑.

### 2.2 9:23-9:30 - 准备永久修复脚本 (3 个版本)

| 版本 | 错误 | 解决 |
|------|------|------|
| **v1** (`_v00786_permanent_fix.ps1`) | 用 `[System.Text.Encoding]::Unicode.GetString` + `Out-File` 加 CRLF 破坏 UTF-16 | (用户没跑, V007.86b 改了) |
| **v2** (`_yonaa_alert_monitor_v0760_clean.xml`) | 直接 `pythonw.exe ...v0760.py --config ... --log-file` 没 `--check-now` | 走 main() 的 else, return 1, 失败 |
| **v3** (`_v00786b_gen_xml_v3.py`) | 用 `alert_monitor.bat` (V007.58 老) | 没用到 v0760.py V007.60 分层 |
| **v4** (`_v00786b_gen_xml_v4.py`) | 用 `alert_monitor_v0760.bat` (V007.62 升级, 有 --check-now) | ✅ **正确版本** |

### 2.3 9:32:00 - UAC 弹窗触发 (用户确认)

```powershell
$psi = New-Object ProcessStartInfo
$psi.FileName = "schtasks"
$psi.Arguments = "/Create /TN \yonaa_alert_monitor /XML D:\...\v2.xml /F"
$psi.Verb = "runas"
[System.Diagnostics.Process]::Start($psi)
```

✅ **用户看到 UAC 弹窗, 点 "是"**
✅ **v2 XML 成功创建计划任务** (`9:36:16` 上次跑 1, `9:40:00` 下次)

### 2.4 9:36-9:50 - 计划任务跑 4 次, 全失败 (结果 1)

```
9:36:16 跑 → 结果 1 (失败)
9:40:00 跑 → 结果 1 (失败)
9:45:01 跑 → 结果 1 (失败)
9:50:01 跑 → 结果 1 (失败)
```

**原因**: v2 XML 没 `--check-now`, 走 main() else, print_help, return 1.

**log 没更新**: 9:33:40 是 daemon 写的, 之后没更新 (任务 print_help 没写 log).

### 2.5 9:53:56 - 临时 daemon 30080 启动

```bash
Start-Process powershell py alert_monitor_v0760.py --daemon --log-file ...
```

✅ **PID 30080 跑着**, 每 5 分钟自动跑, 写 log, 全健康.

---

## 3. 修复 bug 的关键 (V007.86b v4)

### 3.1 v0760.bat 路径修复 (V007.86b)

**老 v0760.bat** (V007.62 创建):
```bat
set "SCRIPT=D:\filework\release-prep-worktree\tools\alert_monitor_v0760.py"  ← 老路径
set "CONFIG=D:\filework\release-prep-worktree\tools\alert_monitor_config.json"
set "LOGFILE=D:\filework\release-prep-worktree\tools\alert_monitor_v0760.log"
"%PYTHONW%" "%SCRIPT%" --config "%CONFIG%" --log-file "%LOGFILE%" --check-now
```

**V007.86b 修复**:
```bat
set "SCRIPT=D:\filework\worktrees\release-prep\tools\alert_monitor_v0760.py"  ← 新路径
set "CONFIG=D:\filework\worktrees\release-prep\tools\alert_monitor_config.json"
set "LOGFILE=D:\filework\worktrees\release-prep\tools\alert_monitor_v0760.log"
"%PYTHONW%" "%SCRIPT%" --config "%CONFIG%" --log-file "%LOGFILE%" --check-now
```

✅ 4 个老路径都改成新路径.

### 3.2 v4 XML 用 alert_monitor_v0760.bat

```xml
<Exec>
  <Command>D:\filework\worktrees\release-prep\tools\alert_monitor_v0760.bat</Command>
  <WorkingDirectory>D:\filework\worktrees\release-prep\tools</WorkingDirectory>
</Exec>
```

✅ .bat 内部有 --check-now + --log-file, 跟 V007.62 原始任务行为一致.

### 3.3 手动测试 v0760.bat (V007.86b 9:40:44)

```bash
py v0760.py --config ... --log-file ... --check-now --force
```

**结果**:
```
[2026-07-17 09:40:44] [V007.60 CHECK] 开始分层监控...
  [run] real_health (interval=300s)
  [run] db_can_write (interval=300s)
  [run] journal_err (interval=300s)
  [run] backend_err (interval=300s)
  [run] core_service_err (interval=300s)
  [run] db_health (interval=900s)
  [run] disk_errors (interval=900s)
  [run] disk_check (interval=1800s)
  [run] disk_usage (interval=1800s)
  ports: 7/7 OK
  systemd: 2 results
  log_service: 2 results
  [HEARTBEAT] lark_app: OK
  [OK] 全部健康
```

✅ 跑通, log 更新, 9:40:44 时间戳正确.

---

## 4. 永久修复路径 (V007.86b v4 升级)

### 4.1 当前状态 (9:53)

| 项 | 状态 |
|---|------|
| 计划任务 `\yonaa_alert_monitor` | **存在, 已启用** |
| 计划任务 Command | `pythonw.exe ...v0760.py --config ... --log-file ...` (v2, **没 --check-now**) |
| 计划任务结果 | **1 (失败)** 持续 |
| daemon 30080 | ✅ 跑着 (临时方案) |
| v0760.bat | ✅ 已改新路径 |
| v4 XML | ✅ 已生成 (`_yonaa_alert_monitor_v0760_clean_v4.xml`) |

### 4.2 用户操作 (1 分钟)

**V007.86b 升级**: 用 v4 XML 替换 v2 任务 (有 --check-now 的 .bat):

```powershell
# 打开管理员 PowerShell
$psi = New-Object System.Diagnostics.ProcessStartInfo
$psi.FileName = "schtasks"
$psi.Arguments = "/Delete /TN \yonaa_alert_monitor /F"
$psi.Verb = "runas"
[System.Diagnostics.Process]::Start($psi) | Out-Null
Start-Sleep 3

$psi2 = New-Object System.Diagnostics.ProcessStartInfo
$psi2.FileName = "schtasks"
$psi2.Arguments = "/Create /TN \yonaa_alert_monitor /XML D:\filework\worktrees\release-prep\tools\_yonaa_alert_monitor_v0760_clean_v4.xml /F"
$psi2.Verb = "runas"
[System.Diagnostics.Process]::Start($psi2) | Out-Null
```

**OR** 简化版 (用 Start-Process):

```powershell
# 打开管理员 PowerShell
Start-Process "schtasks" "/Delete /TN \yonaa_alert_monitor /F" -Verb RunAs -Wait
Start-Process "schtasks" "/Create /TN \yonaa_alert_monitor /XML D:\filework\worktrees\release-prep\tools\_yonaa_alert_monitor_v0760_clean_v4.xml /F" -Verb RunAs -Wait
```

**期望**:
- 任务重建, Command 用 `alert_monitor_v0760.bat`
- 5 分钟后, 上次结果 0 (成功)
- log 显示 `[V007.60 CHECK] 开始分层监控` + `[OK] 全部健康`

### 4.3 升级后 stop daemon

跑通后, **可以**:
```powershell
Get-Process | Where-Object {$_.CommandLine -match "alert_monitor_v0760.*--daemon"} | Stop-Process
```

这样**计划任务接管**, daemon 停.

---

## 5. 总结 (V007.86b 9:53 状态)

| 项 | 状态 |
|---|------|
| **daemon 30080 临时方案** | ✅ 跑着, 5 分钟一次, 写 log |
| **计划任务** | ⚠️ 存在但 v2 XML 失败 (没 --check-now) |
| **v4 XML (有 .bat + --check-now)** | ✅ 已生成, 待用户跑升级命令 |
| **v0760.bat 路径修复** | ✅ 4 个老路径 → 新路径 |
| **Y007.13 远程服务 (172.20.59.7)** | ✅ 健康, 全部 OK |

**V007.86b 完整链路**:
- V007.83 报告: 1 分钟管理员命令
- V007.86 用户报告: 远程监控没了, 13.5 小时 162 次失败
- V007.86 daemon 临时方案: 启动 --daemon 跑 5 分钟一次
- V007.86b 计划任务重建: UAC 弹窗确认, 创建 v2 任务 (但 bug)
- V007.86b 修复 v0760.bat 路径: 4 个老路径 → 新路径
- V007.86b 生成 v4 XML: 用 .bat (有 --check-now)
- V007.86b 重启 daemon 30080: 临时方案再次生效
- **V007.86b 待用户跑 v4 升级命令**

**未来 V007.86c+**:
- 写自动化 pre-commit hook 验证 .xml 文件的 UTF-16 LE 编码
- 写检查脚本验证 .bat 路径跟实际文件存在
- 写 V007.86 教训进 AGENT_INFRA §0.8 计划任务 SOP
