diff --git a/.trae/specs/cross-domain-relationship-permission/spec.md b/.trae/specs/cross-domain-relationship-permission/spec.md
index d445363..a32ebbd 100644
--- a/.trae/specs/cross-domain-relationship-permission/spec.md
+++ b/.trae/specs/cross-domain-relationship-permission/spec.md
@@ -583,6 +583,11 @@ class BoPickService:
 
 ### 2. 鍓嶇鏀瑰姩璇︽儏
 
+> **2026-06-15 閲嶆瀯**: 鍏冩暟鎹┍鍔ㄨ璁?(鐢ㄦ埛閲囩撼)
+> - 涓嶅垱寤?`src/views/Relationship/Create.vue` 涓撶敤瑙嗗浘
+> - 鏀归€犵幇鏈?`MetaForm.vue` 璇嗗埆 `value_help.dual_mode: true` 鈫?娓叉煋 `BoSelectorDualMode`
+> - 浠讳綍瀵硅薄绫诲瀷鐨?FK 瀛楁 YAML 鍔?1 琛岄厤缃嵆鐢熸晥
+
 #### 2.1 BoSelectorDualMode.vue (鏂扮粍浠?
 
 **浣嶇疆**: `src/components/common/ValueHelp/BoSelectorDualMode.vue` (鏂?
@@ -591,6 +596,7 @@ class BoPickService:
 - 椤堕儴鍒囨崲 Tab: "鍒楄〃閫夋嫨" | "鎸夌紪鐮侀€夋嫨"
 - List 妯″紡: 璋?`GET /api/v2/bo/business_object?product_id=P`, 鎸?read scope 杩囨护
 - Pick by Code 妯″紡: 杈撳叆妗?+ 鑷姩琛ュ叏 + 璋?`pick_by_code` API
+- 鍏冩暟鎹┍鍔? 鐢?`MetaForm.vue` 鍦?`field.valueHelp.dual_mode === true` 鏃惰嚜鍔ㄦ覆鏌? 
 **鍏抽敭浠ｇ爜 (绀烘剰)**:
 
@@ -943,14 +949,14 @@ export async function pickBoByCode(code, productId = null) {
 
 > **椋庨櫓**: 鍐欒矾寰勫姞 functional perm 鏍￠獙, 鍙兘璇嫆鍘嗗彶瑙掕壊閰嶇疆 (viewer 绫昏鑹叉湁 dim_scope 浣嗘棤 BO:edit)
 
-#### Phase 1: 鍚庣 + functional perm 鏍￠獙 (杞鍛? [1 鍛╙
+#### Phase 1: 鍚庣 + functional perm 鏍￠獙 (杞鍛? [1 鍛╙ 鉁?宸蹭氦浠?(2026-06-15)
 - 閮ㄧ讲浠ｇ爜, functional perm 鏍￠獙**浠?log warn**, 涓嶅疄闄呮嫆缁? - 鏀堕泦鐢熶骇鐜 1 鍛ㄧ殑"搴旇鎷掍絾鏈嫆" 鐨勮姹?-- 纭鏃犲巻鍙茶鑹查厤缃璇嫆, 杩涘叆 Phase 2
+- 鉁?娴嬭瘯瑕嗙洊: 26 娴?(`meta/tests/test_cross_domain_relation_perm.py`)
 
-#### Phase 2: functional perm 鏍￠獙 (纭嫆缁? [1 鍛╙
-- 鍚敤纭嫆缁?-- 鐩戞帶 403 閿欒鐜? 寮傚父鏃跺洖婊?+#### Phase 2: functional perm 鏍￠獙 (纭嫆缁? [1 鍛╙ 鉁?宸蹭氦浠?(2026-06-15, 绱ф帴 Phase 1)
+- 鉁?缈昏浆 `_WRITE_SCOPE_REL_FUNCTIONAL_PERM_SOFT_WARN` 榛樿鍊?`true 鈫?false` (1 琛?env var flip, 涓嶉噸鍐欓€昏緫)
+- 鈴?鐩戞帶 403 閿欒鐜?(T2.1.3), 寮傚父鏃跺洖婊氭柟娉? `os.environ['WRITE_SCOPE_REL_FUNCTIONAL_PERM_SOFT_WARN'] = 'true' + restart server`
 
 #### Phase 3: 鍓嶇 BoSelectorDualMode 涓婄嚎 [2 鍛╙
 - 浠呭湪鍏崇郴琛ㄥ崟鍚敤, 鍏朵粬琛ㄥ崟缁存寔鐜扮姸
diff --git a/.trae/specs/cross-domain-relationship-permission/tasks.md b/.trae/specs/cross-domain-relationship-permission/tasks.md
index 411359b..63b69f5 100644
--- a/.trae/specs/cross-domain-relationship-permission/tasks.md
+++ b/.trae/specs/cross-domain-relationship-permission/tasks.md
@@ -66,21 +66,27 @@
 
 ## Phase 2: 纭嫆缁濆惎鐢?(1 鍛?
 
+> **2026-06-15 瀹炴柦鐘舵€?*: 鉁?宸插垏纭嫆缁?(env var default `false`)
+> 瀹炴柦鏂瑰紡: env var flip, 涓嶉渶瑕侀噸鍐欎唬鐮侀€昏緫, 淇濈暀杞鍛婃棩蹇楄兘鍔?(鍥為€€/瀹¤鍙屽弸濂?
+
 ### 2.1 functional perm 鏍￠獙 鈫?纭嫆缁? 
-- [ ] **T2.1.1** 淇敼 `write_scope_interceptor.py` 鐨?functional perm 鏍￠獙閫昏緫
-  - 绉婚櫎 Phase 1 鐨?"log warn" 杞鍛?-  - 鏀逛负: `if not has_edit: return False`
-  - 澶辫触鏃惰褰?`INSUFFICIENT_PERMISSION` 閿欒鐮?+- [x] **T2.1.1** 鉁?(2026-06-15) 缈昏浆 `_WRITE_SCOPE_REL_FUNCTIONAL_PERM_SOFT_WARN` 榛樿鍊?`true 鈫?false`
+  - 鏂囦欢: `meta/core/interceptors/write_scope_interceptor.py:120-122`
+  - **涓?*绉婚櫎 log warn 浠ｇ爜, 鑰屾槸 env var 鎺у埗: `soft_warn=True` 璧?log+缁х画; `soft_warn=False` (Phase 2 榛樿) 璧?log+`return False`
+  - 澶辫触鏃? `_log_rel_func_perm_warning(..., 'hard_reject', ...)` + `return False` 瑙﹀彂 `WriteScopeDenied`
+  - 閿欒鐮? `INSUFFICIENT_PERMISSION` (鐢?WriteScopeDenied 鎶涘嚭)
 
-- [ ] **T2.1.2** 鍗曞厓娴嬭瘯鏇存柊
-  - U05 鍦烘櫙浠?"杞鍛婇€氳繃" 鏀逛负 "纭嫆缁?
-  - 鏈熸湜: `INSUFFICIENT_PERMISSION` 閿欒
+- [x] **T2.1.2** 鉁?(2026-06-15) 鍗曞厓娴嬭瘯鏇存柊
+  - `TestDefaultPhaseConfig` 鏂板 `test_default_is_hard_reject` (鏂█榛樿 False)
+  - `TestDefaultPhaseConfig` 鏂板 `test_env_true_switches_to_soft_warn` (env=true 鈫?杞鍛?
+  - `TestU05ViewerRoleSoftWarnVsHardReject` 缁存寔鍙屾ā寮忔祴璇?(patch 鍒?True/False)
+  - 鍏ㄩ儴 26 娴嬮€氳繃
 
-- [ ] **T2.1.3** 鐩戞帶 1 鍛?403 閿欒鐜?-  - 鍏抽敭鎸囨爣: 璺ㄥ煙鍏崇郴鍒涘缓鐨?403 閿欒鐜?+- [ ] **T2.1.3** 鈴?(閮ㄧ讲鍚? 鐩戞帶 1 鍛?403 閿欒鐜?+  - 鍏抽敭鎸囨爣: 璺ㄥ煙鍏崇郴鍒涘缓鐨?403 閿欒鐜?(`rel_func_perm_hard_reject` log 璁℃暟)
   - 闃堝€? > 5% 瑙﹀彂鍥炴粴
-  - 寮傚父鍥炴粴: 鎶?`return False` 鏀瑰洖 `log warn + return True`
+  - **鍥炴粴鏂规硶**: `os.environ['WRITE_SCOPE_REL_FUNCTIONAL_PERM_SOFT_WARN'] = 'true'` + restart server (1 琛?env, 涓嶆敼浠ｇ爜)
 
 ### 2.2 瀹¤鏃ュ織澧炲己
 
@@ -95,42 +101,55 @@
 
 ## Phase 3: 鍓嶇 BoSelectorDualMode 涓婄嚎 (2 鍛?
 
-### 3.1 閫氱敤缁勪欢寮€鍙?+> **2026-06-15 瀹炴柦鐘舵€?*: 鉁?**鍏冩暟鎹┍鍔ㄩ噸鏋勫畬鎴?* (鐢ㄦ埛閲囩撼)
+> 鍏抽敭鍐崇瓥: 涓嶇敤 Relationship 涓撶敤 view, 鏀归€?MetaForm 璇嗗埆 `dual_mode: true` 鈫?浠讳綍 FK 瀛楁 YAML 鍔?1 琛屽嵆鐢熸晥
+
+### 3.1 閫氱敤缁勪欢寮€鍙?(宸插畬鎴?
 
-- [ ] **T3.1.1** 鏂板缓 `src/components/common/ValueHelp/BoListSelector.vue`
+- [x] **T3.1.1** 鉁?`src/components/common/ValueHelp/BoListSelector.vue`
   - 鐜版湁 List 妯″紡缁勪欢, 鎶界涓虹嫭绔嬬粍浠?-  - Props: productId, domainScope
-  - Emits: update:selected
+  - 闆嗘垚 4 绾х骇鑱?+ 璺ㄥ煙娴忚 toggle (Option B)
+  - Props: productId, allowCrossDomain, modelValue
+  - Emits: update:selected, cross-domain-toggled
 
-- [ ] **T3.1.2** 鏂板缓 `src/components/common/ValueHelp/BoCodeSelector.vue`
+- [x] **T3.1.2** 鉁?`src/components/common/ValueHelp/BoCodeSelector.vue`
   - 鏂扮粍浠? Pick by Code 妯″紡
-  - Props: productId
-  - Emits: update:selected
-  - 鍖呭惈閿欒澶勭悊 (404/缃戠粶閿欒/绌?code)
-
-- [ ] **T3.1.3** 鏂板缓 `src/components/common/ValueHelp/BoSelectorDualMode.vue`
-  - 椤跺眰缁勪欢, 鍖呭惈 Tabs 鍒囨崲
-  - Tabs: "鍒楄〃閫夋嫨" / "鎸夌紪鐮侀€夋嫨"
-  - 閫氳繃 ref.open() 鏆撮湶缁欑埗缁勪欢
-
-- [ ] **T3.1.4** 鍦?`src/api/boApi.js` 鏂板 `pickBoByCode(code, productId)` 鏂规硶
-  - URL: `/api/v2/bo/business_object/pick_by_code`
+  - Props: productId, disabled, placeholder
+  - Emits: update:selected, error
+  - 閿欒澶勭悊: MISSING_CODE / BO_NOT_FOUND / UNAUTHORIZED / NETWORK_ERROR
+  - Vitest 8 娴嬪叏缁?+
+- [x] **T3.1.3** 鉁?`src/components/common/ValueHelp/BoSelectorDualMode.vue`
+  - 椤跺眰缁勪欢, 鍖呭惈 Tabs 鍒囨崲 (List / By Code)
+  - 閫氳繃 ref.open() / clearSelection() 鏆撮湶缁欑埗缁勪欢
+  - Vitest 8 娴嬪叏缁?+
+- [x] **T3.1.4** 鉁?`src/services/boService.js` 鏂板 `pickBoByCode(code, productId, options)` 鏂规硶
+  - URL: `/bo/business_object/pick_by_code` (缁?apiV2 鎷兼帴)
   - Method: GET
-  - 澶勭悊 200/404/500 閿欒
+  - 澶勭悊 200/400/401/404
+  - 鏀寔 `include_out_of_scope` (璺ㄥ煙 toggle) + `reason` 瀹¤鍙傛暟
+
+### 3.2 鍏冩暟鎹┍鍔ㄩ泦鎴?(鉁?鏇夸唬鍘?Relationship 涓撶敤 view)
 
-### 3.2 鍏崇郴琛ㄥ崟鏀归€?+- [x] **T3.2.1** 鉁?`src/components/common/MetaForm.vue` 鍔?`dual_mode` 璇嗗埆
+  - `isDualMode(field)` 妫€鏌?`valueHelp.dual_mode === true`
+  - 璇嗗埆鍚庢覆鏌?`BoSelectorDualMode` 鏇夸唬 `ValueHelpField`
+  - 杞彂 `cross-domain-toggled` / `code-error` 浜嬩欢
+  - Vitest 5 娴嬪叏缁?(鍚?ui.dual_mode 璺緞, event forwarding)
 
-- [ ] **T3.2.1** 鏂板缓 `src/views/Relationship/Create.vue`
-  - 琛ㄥ崟瀛楁: source_bo (鍙屾ā寮?, target_bo (鍙屾ā寮?, relation_type, description
-  - 鎻愪氦鏃惰皟 `POST /api/v2/bo/relationship`
-  - 澶勭悊 403/422 閿欒, 鏄剧ず "鎮ㄥ D2 BO 浠呭彲璇? 鏃犳潈鍒涘缓" 鍙嬪ソ鎻愮ず
+- [x] **T3.2.2** 鉁?`src/components/common/DetailPage/DetailPage.vue` 閫忎紶 `dual_mode`
+  - 浠?`f.value_help.dual_mode` 鎴?`f.ui.dual_mode` 娲剧敓
+  - 鍚堝苟鍒?field defs 鐨?`valueHelp` 鍧?+  - 0 鍥炲綊 (鏃?DetailPage 涓撶敤娴嬭瘯)
 
-- [ ] **T3.2.2** 鍏崇郴缂栬緫椤?`src/views/Relationship/Edit.vue` (濡傛棤鍒欐柊寤?
-  - 澶嶇敤 BoSelectorDualMode
-  - 鏀寔淇敼 source/target
+- [x] **T3.2.3** 鉁?`meta/schemas/relationship.yaml` 鍔?`dual_mode: true`
+  - `source_bo_id.ui.dual_mode: true` + `value_help.dual_mode: true`
+  - `target_bo_id.ui.dual_mode: true` + `value_help.dual_mode: true`
+  - **浠讳綍瀵硅薄绫诲瀷鐨?FK 瀛楁** 鍔犺繖涓よ鍗崇敓鏁? **闆朵唬鐮佹敼鍔?*
 
-- [ ] **T3.2.3** 璺敱娉ㄥ唽 `src/router/index.js`
-  - 璺緞: `/relationship/create`, `/relationship/:id/edit`
+- [x] **T3.2.4** 鉁?~~鍒犻櫎~~ `src/views/RelationshipFormView.vue` (鍥炴粴閿欒鏂规)
+  - 鍘熸柟妗堣繚鍙嶅厓鏁版嵁椹卞姩鍘熷垯, 宸插垹闄? 
 ### 3.3 E2E 娴嬭瘯
 
@@ -187,6 +206,53 @@
 
 ---
 
+## 鎺ㄨ繜椤?/ 鍚庣画 TODO (2026-06-15 鐢ㄦ埛鍐冲畾)
+
+> 浠ヤ笅浠诲姟**闈為樆濉?*, 褰撳墠 Phase 1+2+3 宸插畬鏁翠氦浠? 璺ㄥ煙鍏崇郴鍒涘缓鍙敤 (璧?By Code 妯″紡 + OR-edit 闂?銆?+> 鎺ㄨ繜鐞嗙敱: Phase 1+2 OR-edit 闂稿凡淇濋殰鍐欒矾寰勫畨鍏? Phase 3 By Code 妯″紡 + read scope 宸蹭繚闅滆璺緞榛樿瀹夊叏銆?+> 鎺ㄨ繜椤规槸 UX 澧炲己 (B.1+B.2) 鍜屽悎瑙勫寮?(B.3), 缂哄畠浠笉鐮村潖瀹夊叏妯″瀷, 瑙嗕骇鍝佸弽棣堝啀鎺掓湡銆?+
+### B.1 + B.2 (鑱斿悎: 璺ㄥ煙娴忚 toggle 鍚庣鏀寔) [TODO, 鏆備及 1 澶
+
+- [ ] **TB.1** 鍚庣 `business_object` 鍒楄〃 API 鏀寔 `?include_out_of_scope=true&reason=...` 鍙傛暟
+  - 鏂囦欢: `meta/api/bo_api.py` (query 绔偣)
+  - 褰?`include_out_of_scope=true` 涓旂敤鎴锋嫢鏈?`cross_domain_relationship:browse` functional perm 鏃? **缁曡繃** `DataPermissionInterceptor` 鐨?read scope 杩囨护
+  - 褰撶敤鎴?*娌℃湁**璇?perm 浣嗗甫 `include_out_of_scope=true` 鈫?403 PERMISSION_DENIED (涓嶉潤榛樺拷鐣? 鏄庣‘鎷掔粷)
+  - 娉? 涓嶅紩鍏ユ柊 perm 鐨勭畝鍖栨柟妗堜笉鍙彇, 浼氬鑷翠俊鎭硠闇?+
+- [ ] **TB.2** 鏂板 functional perm: `cross_domain_relationship:browse`
+  - YAML 鏀瑰姩: `meta/objects/role.yaml` 鎴?`rls_rules/role.yaml` 鍔?perm 瀹氫箟
+  - 榛樿浠呮巿浜? 璺ㄥ煙鏋舵瀯甯堣鑹?(R-CROSS-ARCH) + 涓氬姟 admin
+  - 涓?`cross_domain_relationship:create` (鎴?`business_object:edit`) 鏄?*姝ｄ氦**鏉冮檺
+  - 娴嬭瘯: 3 娴?(鏈?perm 鍙 / 鏃?perm 403 / 瀹¤璁板綍)
+
+- [ ] **TB.3** 鍗曞厓娴嬭瘯: B.1 + B.2 琛屼负
+  - `meta/tests/test_bo_list_include_out_of_scope.py` (鏂?
+  - 6 娴? perm 鏍￠獙 / reason 蹇呭～ / 瀹¤鍐欏叆 / 涓嶅奖鍝?List 妯″紡榛樿琛屼负 / 閿欒鐮佹牸寮?+
+### B.3 (瀹¤: toggle 浣跨敤杩芥函) [TODO, 鏆備及 0.5 澶
+
+- [ ] **TB.4** 鍐欒矾寰勫璁? `/_diagnostics` 璁板綍璺ㄥ煙 toggle 浣跨敤鎯呭喌
+  - 瀛楁: `{ user_id, ts, object_type, field_key, reason, bo_ids_visited }`
+  - 鍐欏叆鏃舵満: 鐢ㄦ埛鍦?BoListSelector 鍒囨崲 `crossDomainEnabled = true` 鏃?+  - 瑙﹀彂鏂瑰紡: BoSelectorDualMode emit `cross-domain-toggled` 鈫?MetaForm emit 鈫?ObjectDetailPage 鎹曡幏 鈫?璋?audit API
+  - 鍏宠仈鍚庣: `meta/api/audit_api.py` 鍔?`record_toggle_usage` 绔偣
+  - 娴嬭瘯: 3 娴?(toggle 鍐?/ reason 蹇呭～ / 棰戠巼闄愬埗: 鍗曠敤鎴?100 娆?灏忔椂)
+
+### 鍏宠仈鐨勫厓鏁版嵁
+
+- 鍓嶇浠ｇ爜: [BoListSelector.vue:crossDomainEnabled](file:///d:/filework/excel-to-diagram/src/components/common/ValueHelp/BoListSelector.vue) 宸插疄鐜?toggle UI + 閫忎紶 `include_out_of_scope` + `reason` query params, **鍚庣鏀寔鏄己鍙?*
+- 鍚庣 B.1 缂哄け鏃? 鐢ㄦ埛寮€ toggle 鈫?鍚庣蹇界暐鍙傛暟 鈫?鐢ㄦ埛鐪嬩笉鍒?D2 BO 鈫?浣撻獙鍥版儜浣嗗畨鍏ㄦ棤铏?+- 浣曟椂鍚姩: 鐢ㄦ埛鍙嶉 "By Code 妯″紡澶疮" 鎴栨湁浜у搧闇€姹傚笇鏈?D1 manager 鐩存帴鍦?List 妯″紡鐪嬪埌 D2
+
+### 鎺ㄨ繜鍐崇瓥璁板綍
+
+- 2026-06-15: 鐢ㄦ埛涓?Agent 璁ㄨ, 鍐冲畾鎺ㄨ繜 B.1-B.3
+- 鐞嗙敱: 鍐欒矾寰勫凡瀹夊叏 (OR-edit), 璇昏矾寰勯粯璁ゅ畨鍏?(read scope), UX 澧炲己 (璺ㄥ煙 toggle) 鏄?nice-to-have
+- 閲嶆柊璇勪及瑙﹀彂鏉′欢: 1) 璺ㄥ煙鍏崇郴鍒涘缓閲?> 50/澶? 2) 瀹㈡湇鏀跺埌 鈮? 涓?"By Code 妯″紡澶疮" 鍙嶉; 3) 鍚堣瀹¤瑕佹眰 toggle 杩芥函
+
+---
+
 ## 鎬昏: 浠诲姟渚濊禆鍥? 
 ```
diff --git a/components.d.ts b/components.d.ts
index 367fb8f..4d0e420 100644
--- a/components.d.ts
+++ b/components.d.ts
@@ -37,6 +37,9 @@ declare module 'vue' {
     AssociationSelector: typeof import('./src/components/bo/AssociationSelector.vue')['default']
     AuditLog: typeof import('./src/components/common/AuditLog/AuditLog.vue')['default']
     AuditLogDetail: typeof import('./src/components/common/AuditLogDetail/AuditLogDetail.vue')['default']
+    BoCodeSelector: typeof import('./src/components/common/ValueHelp/BoCodeSelector.vue')['default']
+    BoListSelector: typeof import('./src/components/common/ValueHelp/BoListSelector.vue')['default']
+    BoSelectorDualMode: typeof import('./src/components/common/ValueHelp/BoSelectorDualMode.vue')['default']
     BreadcrumbNav: typeof import('./src/components/common/BreadcrumbNav/BreadcrumbNav.vue')['default']
     CenterDomainSelect: typeof import('./src/components/CenterDomainSelect.vue')['default']
     CenterScopeSelector: typeof import('./src/components/CenterScopeSelector.vue')['default']
@@ -50,31 +53,18 @@ declare module 'vue' {
     DetailPage: typeof import('./src/components/common/DetailPage/DetailPage.vue')['default']
     DetailSection: typeof import('./src/components/common/DetailPage/DetailSection.vue')['default']
     Drawer: typeof import('./src/components/common/Drawer/Drawer.vue')['default']
-    EditProfileDialog: typeof import('./src/components/EditProfileDialog.vue')['default']
     ElAlert: typeof import('element-plus/es')['ElAlert']
-    ElAside: typeof import('element-plus/es')['ElAside']
     ElAutocomplete: typeof import('element-plus/es')['ElAutocomplete']
     ElAvatar: typeof import('element-plus/es')['ElAvatar']
     ElBadge: typeof import('element-plus/es')['ElBadge']
-    ElBreadcrumb: typeof import('element-plus/es')['ElBreadcrumb']
-    ElBreadcrumbItem: typeof import('element-plus/es')['ElBreadcrumbItem']
     ElButton: typeof import('element-plus/es')['ElButton']
     ElButtonGroup: typeof import('element-plus/es')['ElButtonGroup']
-    ElCalendar: typeof import('element-plus/es')['ElCalendar']
     ElCard: typeof import('element-plus/es')['ElCard']
-    ElCascader: typeof import('element-plus/es')['ElCascader']
     ElCheckbox: typeof import('element-plus/es')['ElCheckbox']
     ElCheckboxGroup: typeof import('element-plus/es')['ElCheckboxGroup']
-    ElCol: typeof import('element-plus/es')['ElCol']
-    ElCollapse: typeof import('element-plus/es')['ElCollapse']
-    ElCollapseItem: typeof import('element-plus/es')['ElCollapseItem']
     ElCollapseTransition: typeof import('element-plus/es')['ElCollapseTransition']
-    ElColorPicker: typeof import('element-plus/es')['ElColorPicker']
     ElConfigProvider: typeof import('element-plus/es')['ElConfigProvider']
-    ElContainer: typeof import('element-plus/es')['ElContainer']
     ElDatePicker: typeof import('element-plus/es')['ElDatePicker']
-    ElDescriptions: typeof import('element-plus/es')['ElDescriptions']
-    ElDescriptionsItem: typeof import('element-plus/es')['ElDescriptionsItem']
     ElDialog: typeof import('element-plus/es')['ElDialog']
     ElDivider: typeof import('element-plus/es')['ElDivider']
     ElDrawer: typeof import('element-plus/es')['ElDrawer']
@@ -85,49 +75,30 @@ declare module 'vue' {
     ElForm: typeof import('element-plus/es')['ElForm']
     ElFormItem: typeof import('element-plus/es')['ElFormItem']
     ElIcon: typeof import('element-plus/es')['ElIcon']
-    ElImage: typeof import('element-plus/es')['ElImage']
     ElInput: typeof import('element-plus/es')['ElInput']
     ElInputNumber: typeof import('element-plus/es')['ElInputNumber']
     ElLink: typeof import('element-plus/es')['ElLink']
-    ElMain: typeof import('element-plus/es')['ElMain']
-    ElMenu: typeof import('element-plus/es')['ElMenu']
-    ElMenuItem: typeof import('element-plus/es')['ElMenuItem']
     ElOption: typeof import('element-plus/es')['ElOption']
     ElOptionGroup: typeof import('element-plus/es')['ElOptionGroup']
-    ElPageHeader: typeof import('element-plus/es')['ElPageHeader']
     ElPagination: typeof import('element-plus/es')['ElPagination']
     ElPopover: typeof import('element-plus/es')['ElPopover']
     ElProgress: typeof import('element-plus/es')['ElProgress']
     ElRadio: typeof import('element-plus/es')['ElRadio']
     ElRadioGroup: typeof import('element-plus/es')['ElRadioGroup']
-    ElRate: typeof import('element-plus/es')['ElRate']
-    ElResult: typeof import('element-plus/es')['ElResult']
-    ElRow: typeof import('element-plus/es')['ElRow']
-    ElSegmented: typeof import('element-plus/es')['ElSegmented']
     ElSelect: typeof import('element-plus/es')['ElSelect']
     ElSkeleton: typeof import('element-plus/es')['ElSkeleton']
-    ElSlider: typeof import('element-plus/es')['ElSlider']
-    ElSpace: typeof import('element-plus/es')['ElSpace']
-    ElStatistic: typeof import('element-plus/es')['ElStatistic']
     ElStep: typeof import('element-plus/es')['ElStep']
     ElSteps: typeof import('element-plus/es')['ElSteps']
-    ElSubMenu: typeof import('element-plus/es')['ElSubMenu']
     ElSwitch: typeof import('element-plus/es')['ElSwitch']
     ElTable: typeof import('element-plus/es')['ElTable']
     ElTableColumn: typeof import('element-plus/es')['ElTableColumn']
     ElTabPane: typeof import('element-plus/es')['ElTabPane']
     ElTabs: typeof import('element-plus/es')['ElTabs']
     ElTag: typeof import('element-plus/es')['ElTag']
-    ElTimeline: typeof import('element-plus/es')['ElTimeline']
-    ElTimelineItem: typeof import('element-plus/es')['ElTimelineItem']
-    ElTimePicker: typeof import('element-plus/es')['ElTimePicker']
     ElTooltip: typeof import('element-plus/es')['ElTooltip']
-    ElTransfer: typeof import('element-plus/es')['ElTransfer']
     ElTree: typeof import('element-plus/es')['ElTree']
     ElUpload: typeof import('element-plus/es')['ElUpload']
-    ElWatermark: typeof import('element-plus/es')['ElWatermark']
     EmptyState: typeof import('./src/components/common/EmptyState.vue')['default']
-    EnumFieldDisplay: typeof import('./src/components/EnumFieldDisplay.vue')['default']
     EnumSearchHelp: typeof import('./src/components/common/EnumSearchHelp.vue')['default']
     EnumSelect: typeof import('./src/components/common/EnumSelect.vue')['default']
     ErrorBoundary: typeof import('./src/components/common/ErrorBoundary.vue')['default']
@@ -147,6 +118,7 @@ declare module 'vue' {
     ImportDialog: typeof import('./src/components/common/ImportDialog/ImportDialog.vue')['default']
     InlineEditCell: typeof import('./src/components/common/MetaListPage/InlineEditCell.vue')['default']
     InlineEditToolbar: typeof import('./src/components/common/MetaListPage/InlineEditToolbar.vue')['default']
+    LocaleSwitcher: typeof import('./src/components/common/LocaleSwitcher.vue')['default']
     LoginPage: typeof import('./src/components/LoginPage.vue')['default']
     ManagementDimensionSelector: typeof import('./src/components/common/ManagementDimensionSelector/ManagementDimensionSelector.vue')['default']
     MasterDetailLayout: typeof import('./src/components/common/MasterDetailLayout/MasterDetailLayout.vue')['default']
@@ -185,13 +157,11 @@ declare module 'vue' {
     SubNavTabs: typeof import('./src/components/common/SubNavTabs/SubNavTabs.vue')['default']
     TableHeaderFilter: typeof import('./src/components/common/TableHeaderFilter/TableHeaderFilter.vue')['default']
     TopNavHeader: typeof import('./src/components/common/TopNavHeader/TopNavHeader.vue')['default']
-    TourGuide: typeof import('./src/components/TourGuide.vue')['default']
     TreeNode: typeof import('./src/components/TreeNode.vue')['default']
     UserMenu: typeof import('./src/components/common/UserMenu/UserMenu.vue')['default']
     ValidationPanel: typeof import('./src/components/ValidationPanel.vue')['default']
     ValueHelpField: typeof import('./src/components/common/ValueHelpField.vue')['default']
     ValueHelpSelector: typeof import('./src/components/common/ConditionRuleEditor/ValueHelpSelector.vue')['default']
-    VersionContextSelector: typeof import('./src/components/VersionContextSelector/VersionContextSelector.vue')['default']
   }
   export interface GlobalDirectives {
     vLoading: typeof import('element-plus/es')['ElLoadingDirective']
diff --git a/docs/auth/role-templates.md b/docs/auth/role-templates.md
index 0c13aff..973efa3 100644
--- a/docs/auth/role-templates.md
+++ b/docs/auth/role-templates.md
@@ -309,3 +309,30 @@ A: 璁剧幆澧冨彉閲?`WRITE_SCOPE_AUDIT_ONLY=true` 鍗冲彲杞鍛婃ā寮? 绔嬪嵆
 - `auth-permission-system/write-scope-interceptor.md` 鈥?鎷︽埅鍣ㄦ妧鏈粏鑺? - `auth-permission-system/role-migration-guide.md` 鈥?鐜版湁瑙掕壊杩佺Щ鎸囧崡
 - SAP Authorization Object S_TCODE / Oracle Function Security 鈥?澶撮儴浜у搧瀵圭収
+
+---
+
+## 8. 璺ㄩ鍩熷叧绯昏鑹?(V1.2.0 鏂板)
+
+> Spec: .trae/specs/cross-domain-relationship-permission/spec.md
+
+### 8.1 R-CROSS-D1 鈥?D1 鍩熻礋璐ｄ汉
+
+- product: [浜у搧 P id], inherit=true
+- domain: [D1 id], inherit=true (鏍稿績)
+- functional perm: BO:read/create/update/delete + relationship:read/create/update/delete
+
+### 8.2 R-CROSS-D2 鈥?D2 鍩熻礋璐ｄ汉 (瀵圭О R-CROSS-D1)
+
+- domain: [D2 id], inherit=true
+
+### 8.3 R-CROSS-ARCH 鈥?璺ㄥ煙鏋舵瀯甯?+
+- domain: [D1, D2, D3, D4], inherit=true (澶氬煙)
+- 閫傜敤: 璺ㄥ煙鍏崇郴璁捐, 涓嶉渶瑕佹寜缂栫爜閫夋嫨妯″紡
+
+### 8.4 鍏抽敭鍐崇瓥
+
+- U1 鍒涘缓 D1-D2 鍏崇郴, 閰?[D1] 鍗冲彲 (OR-edit 璇箟)
+- 涓嶉渶瑕佽法鍩熸灦鏋勫笀 (U1/U2 鍚勮嚜鍗冲彲)
+- functional perm 鏍￠獙: Phase 1 杞鍛? Phase 2 纭嫆缁?diff --git a/meta/api/bo_api.py b/meta/api/bo_api.py
index 58b63b8..d73397b 100644
--- a/meta/api/bo_api.py
+++ b/meta/api/bo_api.py
@@ -382,6 +382,10 @@ def query_bo(object_type):
     # 鐩存帴浣跨敤crud_query action
     result = bo.execute(object_type, 'crud_query', query_params)
 
+    if not result.success:
+        logger.warning("[query_bo] query failed: object_type=%s msg=%s errors=%s",
+                       object_type, result.message, result.errors)
+
     logger.info(f"[query_bo] result.success={result.success}, result.data type={type(result.data)}, len={len(result.data) if hasattr(result.data, '__len__') else 'N/A'}")
 
     # 杞崲鏁版嵁鏍煎紡浠ュ尮閰嶅墠绔湡鏈?diff --git a/meta/api/management_dimension_api.py b/meta/api/management_dimension_api.py
index f5785be..7dab3f6 100644
--- a/meta/api/management_dimension_api.py
+++ b/meta/api/management_dimension_api.py
@@ -8,7 +8,7 @@
 import logging
 import os
 from functools import wraps
-from typing import Any, Dict, Optional
+from typing import Any, Dict, List, Optional, Set
 
 from flask import Blueprint, g, jsonify, request
 
@@ -22,13 +22,16 @@ from meta.services.management_dimension_engine import (
     ManagementDimensionEngine,
     ConditionEvaluator,
 )
+from meta.services.dimension_scope_engine import DimensionScopeEngine
 
 _PARENT_INFO_MAP = {
     'version': ('product', 'products', 'product_id', 'name'),
     'domain': ('version', 'versions', 'version_id', 'name'),
-    'sub_domain': ('domain', 'domains', 'domain_id', 'domain_name'),
-    'service_module': ('sub_domain', 'sub_domains', 'sub_domain_id', 'sub_domain_name'),
-    'business_object': ('service_module', 'service_modules', 'service_module_id', 'module_name'),
+    # [FIX 2026-06-15] parent_display 瀛楁鍚嶇籂姝? 瀹為檯 schema 涓墍鏈夊瓙琛ㄩ兘鐢?'name' 鑰岄潪 'parent_name'
+    # 涔嬪墠鐢?domain_name / sub_domain_name / module_name 鍦?SQL JOIN 鏃舵姤 "no such column"
+    'sub_domain': ('domain', 'domains', 'domain_id', 'name'),
+    'service_module': ('sub_domain', 'sub_domains', 'sub_domain_id', 'name'),
+    'business_object': ('service_module', 'service_modules', 'service_module_id', 'name'),
 }
 
 logger = logging.getLogger(__name__)
@@ -58,6 +61,202 @@ def _is_testing():
     return os.environ.get("FLASK_ENV") == "testing" or os.environ.get("TESTING") == "1"
 
 
+def _get_user_dim_scope_ids(user_id: int, dimension_id: str) -> Optional[Set[int]]:
+    """[FIX 2026-06-15] 鏀堕泦鐢ㄦ埛鍦ㄦ寚瀹?dimension 涓婂彲瑙佺殑 id 闆嗗悎
+
+    鏁版嵁婧愰摼璺?
+        user_group_members 鈫?group_roles 鈫?role_dimension_scopes
+        鈫?DimensionScopeEngine.expand_dimension_values(role_id) 鈫?{dim: set(ids)}
+
+    閾惧紡鎵╁睍:
+        HIERARCHY_CHAIN 鍙埌 sub_domain, 浣?service_module/business_object
+        浠嶉渶鎸?FK 閾?(sub_domain 鈫?service_module 鈫?business_object) 鎵嬪姩鎵╁睍
+
+    杩斿洖:
+        - None: 璇ョ敤鎴?*娌℃湁浠讳綍瑙掕壊**瀵逛换浣?dimension 璁句簡 scope 鈫?涓嶈繃婊?(鍏煎鏃ц涓?
+        - set(): 璇ョ敤鎴?*鏈夎鑹查厤缃簡 scope**, 浣?expand 鍚庝负绌?鈫?涓ユ牸 0 鏉″彲瑙?+        - set([...]): 璇ョ敤鎴峰彲瑙佺殑 id 闆嗗悎 (鏉ヨ嚜 dim_values / inherit_children / FK 閾炬墿灞?
+
+    杈圭晫:
+        - admin 鐢ㄦ埛 / '*' 鏉冮檺: 涓嶈皟姝ゅ嚱鏁?(璋冪敤鏂瑰垽鏂悗璺宠繃)
+        - 鏁版嵁鏍煎紡寮傚父 (e.g. dim_values=NULL): 鐢?inherit_children 鍏滃簳
+        - inherit_children=0 涔熷己鍒舵墿灞?(鍥犱负鐢ㄦ埛鏈?BO:edit 绛変笅灞?perm)
+    """
+    global _data_source
+    if _data_source is None:
+        return None
+
+    try:
+        # 1. user 鈫?group_ids
+        cursor = _data_source.execute(
+            "SELECT group_id FROM user_group_members WHERE user_id = ?",
+            [user_id]
+        )
+        group_ids = [r[0] for r in cursor.fetchall()]
+        if not group_ids:
+            return None
+
+        # 2. groups 鈫?role_ids (DISTINCT)
+        placeholders = ",".join("?" for _ in group_ids)
+        cursor = _data_source.execute(
+            f"SELECT DISTINCT role_id FROM group_roles WHERE group_id IN ({placeholders})",
+            group_ids
+        )
+        role_ids = [r[0] for r in cursor.fetchall()]
+        if not role_ids:
+            return None
+
+        # 3. 瀵规瘡涓?role 璋?expand_dimension_values, union 璇?dimension 鐨?id
+        engine = DimensionScopeEngine(_data_source)
+        all_ids: Set[int] = set()
+        has_any_scope = False
+        any_dimension_scope: Dict[str, Set[int]] = {}
+
+        for role_id in role_ids:
+            try:
+                expanded = engine.expand_dimension_values(role_id)
+            except Exception:
+                expanded = {}
+
+            # 鏀堕泦璇?role 鍏ㄩ儴 dimension 鐨?scope (鐢ㄤ簬鍚庣画 FK 閾炬墿灞?
+            for dim, ids in expanded.items():
+                if ids:
+                    has_any_scope = True
+                    if dim not in any_dimension_scope:
+                        any_dimension_scope[dim] = set()
+                    any_dimension_scope[dim].update(ids)
+
+        if not has_any_scope:
+            return None
+
+        # 4. 濡傛灉鐩爣 dimension 鐩存帴鏈?scope, 鐩存帴鐢?+        if dimension_id in any_dimension_scope and any_dimension_scope[dimension_id]:
+            return any_dimension_scope[dimension_id]
+
+        # 5. [FIX 2026-06-15] 鐩爣 dimension 涓嶅湪 scope 閾句腑, 浣?user 鍦ㄦ洿楂?dim 鏈?scope
+        #    閫氳繃 FK 閾炬墜鍔ㄦ墿灞?(product 鈫?version 鈫?domain 鈫?sub_domain 鈫?service_module 鈫?business_object)
+        #    寮哄埗鎵╁睍: 鍗充究 inherit_children=0, 鍥犱负 user 閰嶄簡 BO:edit 绛変笅灞?perm, 蹇呴』鐪嬪埌涓嬪眰
+        return _expand_via_fk_chain(dimension_id, any_dimension_scope, _data_source)
+    except Exception as e:
+        logger.error(f"鑾峰彇鐢ㄦ埛 dim scope 澶辫触 [user_id={user_id}, dim={dimension_id}]: {e}")
+        return None
+
+
+def _expand_via_fk_chain(
+    target_dim: str,
+    scope_by_dim: Dict[str, Set[int]],
+    ds
+) -> Set[int]:
+    """[FIX 2026-06-15] 閫氳繃 FK 閾炬妸楂樺眰 dim scope 鎵╁睍鍒扮洰鏍?dim
+
+    閾捐矾: product 鈫?version 鈫?domain 鈫?sub_domain 鈫?service_module 鈫?business_object
+    姣忎釜 step 鐢?parent_fk 瀛楁鍙嶅悜鏌ュ瓙琛?(瀛愯〃.parent_fk IN current_ids)
+    """
+    # [FIX 2026-06-15] 瀹屾暣 6 绾?FK 閾?(engine 鐨?HIERARCHY_CHAIN 鍙埌 sub_domain)
+    CHAIN = [
+        # (dim_name, child_table, parent_fk_on_child_table)
+        ('product', None, None),                          # root, 鏃?parent
+        ('version', 'versions', 'product_id'),
+        ('domain', 'domains', 'version_id'),
+        ('sub_domain', 'sub_domains', 'domain_id'),
+        ('service_module', 'service_modules', 'sub_domain_id'),
+        ('business_object', 'business_objects', 'service_module_id'),
+    ]
+
+    if target_dim not in [c[0] for c in CHAIN]:
+        return set()
+
+    # 1. 鎵捐捣濮嬩綅缃? 浠庢渶楂?(i=0) 鎵剧涓€涓湁 scope 鐨?dim, 鎴栧埌杈?target_dim
+    current_ids: Optional[Set[int]] = None
+    start_idx = -1
+    for i, (dim, _, _) in enumerate(CHAIN):
+        if dim in scope_by_dim and scope_by_dim[dim]:
+            current_ids = set(scope_by_dim[dim])
+            start_idx = i
+            break
+
+    if current_ids is None:
+        # 娌℃湁浠讳綍 dim 鏈?scope, 宸茬粡涓嶄細鍒拌繖涓€姝?(caller has_any_scope check)
+        return set()
+
+    # 1.5 鏂瑰悜妫€鏌? target 蹇呴』 鈮?start_idx (鍙兘鍚戜笅璧? 涓嶈兘鍚戜笂)
+    target_idx = next(i for i, (d, _, _) in enumerate(CHAIN) if d == target_dim)
+    if target_idx < start_idx:
+        # target 鍦?start 涔嬩笂, 鏃犳硶鍚戜笂鎵╁睍 (濡? scope=domain, target=product)
+        return set()
+
+    # 2. 浠?start_idx 寮€濮? 娌?FK 閾惧悜涓嬭蛋, 姣忔鏌?child_table
+    for i in range(start_idx, len(CHAIN)):
+        dim, child_table, parent_fk = CHAIN[i]
+
+        if dim == target_dim:
+            # 鍒拌揪鐩爣
+            return current_ids
+
+        # 鍚﹀垯缁х画鍚戜笅璧颁竴姝? 鐢?current_ids 鏌ヤ笅涓€绾?+        next_i = i + 1
+        if next_i >= len(CHAIN):
+            break
+        next_dim, next_table, next_fk = CHAIN[next_i]
+        if not next_table or not next_fk:
+            continue
+
+        # 瑕嗙洊涓棿 dim 鐨?scope (濡傛灉瀛樺湪, 鐩存帴鐢?scope 鏇挎崲)
+        if next_dim in scope_by_dim and scope_by_dim[next_dim]:
+            current_ids = set(scope_by_dim[next_dim])
+        else:
+            # 鐢?parent_fk 鍙嶆煡瀛愯〃
+            current_ids = _query_child_ids(ds, next_table, next_fk, current_ids)
+
+    return set()
+
+
+def _query_child_ids(ds, child_table: str, parent_fk: str, parent_ids: Set[int]) -> Set[int]:
+    """鏌ュ瓙琛?id 闆嗗悎 (child_table.parent_fk IN parent_ids)"""
+    if not parent_ids:
+        return set()
+    placeholders = ",".join("?" for _ in parent_ids)
+    try:
+        cursor = ds.execute(
+            f"SELECT id FROM {child_table} WHERE {parent_fk} IN ({placeholders})",
+            list(parent_ids)
+        )
+        return {row[0] for row in cursor.fetchall()}
+    except Exception as e:
+        logger.error(f"_query_child_ids failed: table={child_table} parent_fk={parent_fk} err={e}")
+        return set()
+
+
+def _parse_id_field(raw) -> List[int]:
+    """瑙ｆ瀽 id 瀛楁: 鏀寔 JSON 瀛楃涓?/ list / None"""
+    if raw is None:
+        return []
+    if isinstance(raw, (list, tuple)):
+        return [int(x) for x in raw if str(x).isdigit()]
+    if isinstance(raw, str):
+        try:
+            import json as _json
+            parsed = _json.loads(raw)
+            if isinstance(parsed, list):
+                return [int(x) for x in parsed if str(x).isdigit()]
+        except Exception:
+            # fallback: 鎸夐€楀彿鍒嗛殧
+            return [int(x.strip()) for x in raw.split(",") if x.strip().isdigit()]
+    return []
+
+
+def _is_admin_user() -> bool:
+    """鍒ゆ柇褰撳墠鐢ㄦ埛鏄惁 admin (缁曡繃 dim scope)"""
+    if not hasattr(g, "current_user") or not g.current_user:
+        return False
+    perms = g.current_user.get("permissions", []) or []
+    if "*" in perms or "admin" in perms:
+        return True
+    if g.current_user.get("is_admin") is True:
+        return True
+    return False
+
+
 def _login_required(f):
     """鑷畾涔?login_required 瑁呴グ鍣紝鏀寔娴嬭瘯妯″紡"""
     @wraps(f)
@@ -278,6 +477,36 @@ def get_dimension_instances(dimension_id: str):
                             where_clause = f"WHERE main.{field_name} IN ({placeholders})"
                         params.extend(filter_values)
 
+        # [FIX 2026-06-15] 搴旂敤鐢ㄦ埛 dim scope 杩囨护
+        # 涓氬姟鑳屾櫙: ValueHelp 寮圭獥 (鏉ユ簮/鐩爣 4 绾х骇鑱? 闇€鎸夌敤鎴?role 鐨?dimension scope 杩囨护
+        # 淇鍓? 瀹屽叏涓嶈繃婊? TEST888 閰?dim_scope=domain=703 鍗寸湅鍒板叏閮?484 涓?domain
+        # 淇鍚? 浠呰繑鍥炵敤鎴?role scope 瑕嗙洊鐨?instance (admin 璺宠繃)
+        if not _is_admin_user() and hasattr(g, "current_user") and g.current_user:
+            user_id = g.current_user.get("user_id")
+            if user_id:
+                scope_ids = _get_user_dim_scope_ids(int(user_id), dimension_id)
+                if scope_ids is not None:
+                    # [FIX 2026-06-15] scope_ids 鏄┖闆?vs 闈炵┖闆?鍖哄垎:
+                    #   闈炵┖ 鈫?IN (ids)
+                    #   绌洪泦 (role 鏈?scope 浣?expand 鍚庢棤 ids) 鈫?鏃犲彲瑙?(杩斿洖绌哄垎椤?
+                    if scope_ids:
+                        id_placeholders = ','.join(['?' for _ in scope_ids])
+                        if where_clause:
+                            where_clause += f" AND main.id IN ({id_placeholders})"
+                        else:
+                            where_clause = f"WHERE main.id IN ({id_placeholders})"
+                        params.extend(list(scope_ids))
+                    else:
+                        # 绌洪泦: 寮轰竴鑷?0 鏉″彲瑙?+                        if where_clause:
+                            where_clause += " AND 1=0"
+                        else:
+                            where_clause = "WHERE 1=0"
+                        logger.info(
+                            f"[dim-scope] user_id={user_id} dim={dimension_id} "
+                            f"role scope 閰嶇疆浣?expand 鍚庝负绌? 寮哄埗 0 鏉″彲瑙?
+                        )
+
         select_fields = f"main.id, main.{code_field}, main.{display_field}"
         from_clause = f"FROM {table_name} main"
         count_from = f"FROM {table_name} main"
diff --git a/meta/api/value_help_api.py b/meta/api/value_help_api.py
index 17c68e1..cbe3961 100644
--- a/meta/api/value_help_api.py
+++ b/meta/api/value_help_api.py
@@ -3,6 +3,7 @@ import json
 from meta.core.value_help_providers import get_provider
 from meta.core.models import ValueHelpSource
 from meta.services.auth_middleware import login_required
+from meta.services.bo_pick_service import BoPickService
 
 value_help_bp = Blueprint("value_help", __name__)
 
@@ -131,3 +132,93 @@ def resolve_value_help(source_type, source_id):
         return jsonify({"success": True, "data": result})
     except Exception as e:
         return jsonify({"success": False, "error": str(e)}), 500
+
+
+# =============================================================================
+# [V1.2.0 2026-06-15] 璺ㄩ鍩熷叧绯?- BO Pick by Code 绔偣
+# Spec: .trae/specs/cross-domain-relationship-permission/spec.md
+#
+# 璁捐鐩殑: 瑙ｅ喅"鍓嶇 ValueHelp List 妯″紡鐪嬩笉鍒?D2 BO" 鐨勬閿?
+#   - List 妯″紡璧?read scope 杩囨护 (鐜版湁), U1 鐪嬩笉鍒?D2 BO
+#   - Pick by Code 妯″紡鎸夌紪鐮佺簿纭€? 涓嶅簲鐢?read scope 杩囨护
+#   - 浣嗕粛鍙?WriteScopeInterceptor 鍐欐潈闄愭牎楠?(OR-edit, 涓嶇粫杩?
+#
+# 瀹夊叏淇濊瘉:
+#   1. 浠嶉渶 dev-login cookie 閴存潈 (login_required)
+#   2. product_id 蹇呭～ (OQ2 鍐崇瓥), 闃茶法浜у搧璇€?+#   3. 浠呰繑鍥為潪鏁忔劅瀛楁 (id, code, name, description, version_id, service_module_id)
+#   4. 涓嶈繑鍥?owner_id, created_by, updated_by 绛夋晱鎰熷瓧娈?+# =============================================================================
+@value_help_bp.route("/api/v2/bo/business_object/pick_by_code", methods=["GET"])
+@login_required
+def pick_bo_by_code():
+    """[V1.2.0] 鎸夌紪鐮佺簿纭€夊彇 BO (涓嶅簲鐢?read scope 杩囨护)
+
+    Query:
+        code: BO 缂栫爜 (蹇呭～, 濡?BO_B_001)
+        product_id: 浜у搧 ID (蹇呭～, OQ2 鍐崇瓥, 闃茶法浜у搧璇€?
+
+    Response:
+        200: {success: true, data: {id, code, name, description, version_id, service_module_id}}
+        400: {success: false, error_code: 'MISSING_CODE' | 'MISSING_PRODUCT_ID' | 'INVALID_PRODUCT_ID'}
+        404: {success: false, error_code: 'BO_NOT_FOUND'}
+    """
+    code = request.args.get("code", "").strip()
+    product_id = request.args.get("product_id", "").strip()
+
+    if not code:
+        return jsonify({
+            "success": False,
+            "error_code": "MISSING_CODE",
+            "message": "code 鍙傛暟涓嶈兘涓虹┖",
+        }), 400
+
+    if not product_id:
+        return jsonify({
+            "success": False,
+            "error_code": "MISSING_PRODUCT_ID",
+            "message": "product_id 鍙傛暟蹇呭～ (OQ2 鍐崇瓥, 闃茶法浜у搧璇€?",
+        }), 400
+
+    try:
+        product_id_int = int(product_id)
+    except ValueError:
+        return jsonify({
+            "success": False,
+            "error_code": "INVALID_PRODUCT_ID",
+            "message": f"product_id 蹇呴』鏄暣鏁? 褰撳墠: {product_id}",
+        }), 400
+
+    # 濮旀淳缁?BoPickService
+    bo = BoPickService.pick_by_code(code=code, product_id=product_id_int)
+    if bo is None:
+        return jsonify({
+            "success": False,
+            "error_code": "BO_NOT_FOUND",
+            "message": f"BO 缂栫爜 {code} 鍦?product {product_id} 涓嬩笉瀛樺湪",
+        }), 404
+
+    return jsonify({"success": True, "data": bo})
+
+
+@value_help_bp.route("/api/v2/bo/business_object/<int:bo_id>", methods=["GET"])
+@login_required
+def pick_bo_by_id(bo_id: int):
+    """[V1.2.0] 鎸?ID 绮剧‘閫夊彇 BO (涓嶅簲鐢?read scope 杩囨护, 浠呭惈鍩烘湰瀛楁)
+
+    Path:
+        bo_id: BO ID
+
+    Response:
+        200: {success: true, data: {id, code, name, ...}}
+        404: {success: false, error_code: 'BO_NOT_FOUND'}
+    """
+    bo = BoPickService.pick_by_id(bo_id=bo_id)
+    if bo is None:
+        return jsonify({
+            "success": False,
+            "error_code": "BO_NOT_FOUND",
+            "message": f"BO ID {bo_id} 涓嶅瓨鍦?,
+        }), 404
+
+    return jsonify({"success": True, "data": bo})
diff --git a/meta/core/interceptors/data_permission_interceptor.py b/meta/core/interceptors/data_permission_interceptor.py
index 987bbd1..06f9338 100644
--- a/meta/core/interceptors/data_permission_interceptor.py
+++ b/meta/core/interceptors/data_permission_interceptor.py
@@ -39,9 +39,11 @@ class DataPermissionInterceptor(Interceptor):
             return
 
         if not AUTH_ENABLED:
+            logger.warning(f'[DPI-DEBUG] AUTH_ENABLED=False, skipping all permission checks')
             return
 
         if self._is_admin(context):
+            logger.info(f'[DPI-DEBUG] user={context.user_id} is_admin=True, skipping dim scope')
             return
 
         # [FIX v1.0.2 + v1.0.5 2026-06-10] 浼樺厛搴旂敤 role_dimension_scopes 娲剧敓鏉′欢
@@ -63,6 +65,7 @@ class DataPermissionInterceptor(Interceptor):
         #   )
         #   OR (owner_id = $user_id)                  -- 鑷繁 owner 濮嬬粓鍙
         dimension_applied = self._apply_dimension_scope_filter(context)
+        logger.info(f'[DPI-DEBUG] user={context.user_id} object_type={context.object_type} dimension_applied={dimension_applied} query_conditions={context.extra.get("query_conditions", [])}')
 
         if dimension_applied:
             # Dimension scope 宸插簲鐢? 缁х画鍙犲姞 visibility/owner scope (AND 鍏崇郴)
@@ -398,7 +401,8 @@ class DataPermissionInterceptor(Interceptor):
     def _parse_scope_expression(expr: str):
         import re
 
-        or_parts = re.split(r'\s+OR\s+', expr, flags=re.IGNORECASE)
+        # [FIX] 鎸夐《灞?OR 鍒嗗壊锛屽拷鐣ユ嫭鍙峰唴鐨?OR锛堥伩鍏嶆媶鍒嗗瓙鏌ヨ鍐呴儴鐨?OR锛?+        or_parts = DataPermissionInterceptor._split_top_level_or(expr)
         if len(or_parts) > 1:
             or_group = []
             for part in or_parts:
@@ -407,6 +411,41 @@ class DataPermissionInterceptor(Interceptor):
 
         return [DataPermissionInterceptor._parse_simple_condition(expr.strip())]
 
+    @staticmethod
+    def _split_top_level_or(expr: str) -> List[str]:
+        """鎸夐《灞?OR 鍒嗗壊琛ㄨ揪寮忥紝蹇界暐鎷彿鍐呯殑 OR
+
+        绀轰緥:
+          "a = 1 OR b = 2" 鈫?["a = 1", "b = 2"]
+          "x IN (SELECT ... WHERE a OR b)" 鈫?["x IN (SELECT ... WHERE a OR b)"]
+        """
+        parts = []
+        depth = 0
+        current = []
+        i = 0
+        upper = expr.upper()
+        while i < len(expr):
+            if expr[i] == '(':
+                depth += 1
+                current.append(expr[i])
+                i += 1
+            elif expr[i] == ')':
+                depth -= 1
+                current.append(expr[i])
+                i += 1
+            elif depth == 0 and upper[i:i+4] == ' OR ' and (i == 0 or not upper[i-1].isalnum()):
+                # 椤跺眰 OR
+                parts.append(''.join(current).strip())
+                current = []
+                i += 4  # skip ' OR '
+            else:
+                current.append(expr[i])
+                i += 1
+        remainder = ''.join(current).strip()
+        if remainder:
+            parts.append(remainder)
+        return parts
+
     @staticmethod
     def _parse_simple_condition(expr: str):
         import re
diff --git a/meta/core/interceptors/persistence_interceptor.py b/meta/core/interceptors/persistence_interceptor.py
index a968fa9..4f69f93 100644
--- a/meta/core/interceptors/persistence_interceptor.py
+++ b/meta/core/interceptors/persistence_interceptor.py
@@ -943,7 +943,8 @@ class PersistenceInterceptor(Interceptor):
 
             return ActionResult(success=True, data=records, total=total)
         except Exception as e:
-            logger.error(f"[_do_list] Error: {e}")
+            logger.error("[_do_list] Error: %s, object_type=%s", e,
+                         meta_object.id if meta_object else '?')
             return ActionResult(success=False, message=str(e), errors=[str(e)])
 
     def _execute_for_list(self, registry, sql, params, use_fresh):
diff --git a/meta/core/query_builder.py b/meta/core/query_builder.py
index ee07845..3389ef6 100644
--- a/meta/core/query_builder.py
+++ b/meta/core/query_builder.py
@@ -555,10 +555,14 @@ class QueryBuilder:
             params.append(cond.value)
             return "LOWER({0}) LIKE LOWER(?)".format(cond.field), params
         elif cond.operator == QueryOperator.IN:
+            if not cond.values:
+                return "1=0", params
             placeholders = ", ".join(["?"] * len(cond.values))
             params.extend(cond.values)
             return "{0} IN ({1})".format(cond.field, placeholders), params
         elif cond.operator == QueryOperator.NOT_IN:
+            if not cond.values:
+                return "1=1", params
             placeholders = ", ".join(["?"] * len(cond.values))
             params.extend(cond.values)
             return "{0} NOT IN ({1})".format(cond.field, placeholders), params
diff --git a/meta/schemas/relationship.yaml b/meta/schemas/relationship.yaml
index a8c7b24..7f36a73 100644
--- a/meta/schemas/relationship.yaml
+++ b/meta/schemas/relationship.yaml
@@ -749,6 +749,9 @@ fields:
       depends_on: source_service_module_id
       cascade_group: relationship
       cascade_level: 5
+      # [V1.2.0 2026-06-15] 璺ㄥ煙鍏崇郴 BoSelectorDualMode
+      # 鍚敤 List / Pick by Code 鍙屾ā寮?ValueHelp (璺ㄥ煙閫冪敓鍙?
+      dual_mode: true
     semantics:
       meaning: 鍏崇郴鐨勬簮绔笟鍔″璞?       parent_key: true
@@ -767,6 +770,10 @@ fields:
         display_field: name
         code_field: code
         apply_target_permissions: false
+      # [V1.2.0 2026-06-15] 璺ㄥ煙鍏崇郴鍙屾ā寮?ValueHelp 寮€鍏?+      # dual_mode: true  鈫? 娓叉煋 BoSelectorDualMode (List 妯″紡濂?read scope, By Code 妯″紡閫冪敓鍙?
+      # 璇﹁: .trae/specs/cross-domain-relationship-permission/spec.md (Option B)
+      dual_mode: true
       behavior:
         validation: true
         binding_strength: strict
@@ -798,6 +805,8 @@ fields:
       depends_on: target_service_module_id
       cascade_group: relationship
       cascade_level: 5
+      # [V1.2.0 2026-06-15] 璺ㄥ煙鍏崇郴 BoSelectorDualMode
+      dual_mode: true
     semantics:
       meaning: 鍏崇郴鐨勭洰鏍囩涓氬姟瀵硅薄
       parent_key: true
@@ -816,6 +825,8 @@ fields:
         display_field: name
         code_field: code
         apply_target_permissions: false
+      # [V1.2.0 2026-06-15] 璺ㄥ煙鍏崇郴鍙屾ā寮?ValueHelp 寮€鍏?+      dual_mode: true
       behavior:
         validation: true
         binding_strength: strict
diff --git a/meta/services/cascade_service.py b/meta/services/cascade_service.py
index 6249206..2bc648e 100644
--- a/meta/services/cascade_service.py
+++ b/meta/services/cascade_service.py
@@ -91,6 +91,14 @@ class HierarchyConfigLoader:
                 return CascadeStrategy.RESTRICT
         return cls.get_cascade_strategy_from_associations(parent_type, child_type)
 
+    @classmethod
+    def get_table_name(cls, object_type: str, hierarchy_id: str = 'biz_hierarchy') -> Optional[str]:
+        """浠庡眰绾ч厤缃幏鍙栬〃鍚嶏紙姣?registry 鏇村彲闈狅紝涓嶄緷璧?schema 鍔犺浇椤哄簭锛?""
+        level = cls.get_level_by_object(object_type, hierarchy_id)
+        if level and level.get('table_name'):
+            return level.get('table_name')
+        return None
+
     @classmethod
     def get_type_order(cls) -> List[str]:
         levels = cls.get_levels('biz_hierarchy')
diff --git a/meta/services/data_permission_filter.py b/meta/services/data_permission_filter.py
index 47dba5c..6e934fb 100644
--- a/meta/services/data_permission_filter.py
+++ b/meta/services/data_permission_filter.py
@@ -5,10 +5,13 @@
 鍦ㄦ煡璇㈠眰娉ㄥ叆鏁版嵁鏉冮檺鏉′欢锛屽疄鐜伴€忔槑杩囨护
 """
 
+import logging
 from typing import List, Dict, Any, Optional
 from meta.core.query_builder import QueryCondition, QueryOperator
 from meta.services.data_permission_service import DataPermissionService
 
+logger = logging.getLogger(__name__)
+
 
 class DataPermissionFilter:
     def __init__(self, data_source):
@@ -19,19 +22,22 @@ class DataPermissionFilter:
                      conditions: List[QueryCondition]) -> List[QueryCondition]:
         from meta.services.auth_middleware import is_admin, get_current_user
 
-        print(f"[DataPermFilter] apply_filter called: object_type={object_type}, user_id={user_id}")
-        current_user = get_current_user()
-        print(f"[DataPermFilter] Current user: {current_user}")
-
         if is_admin():
-            print(f"[DataPermFilter] User is admin, skipping filter")
             return conditions
 
-        allowed_ids = self.perm_service.get_allowed_resource_ids(user_id, object_type)
-        print(f"[DataPermFilter] Allowed IDs for {object_type}: {allowed_ids}")
+        try:
+            allowed_ids = self.perm_service.get_allowed_resource_ids(user_id, object_type)
+        except Exception as e:
+            logger.error(f"[DataPermFilter] get_allowed_resource_ids failed for "
+                         f"user_id={user_id} object_type={object_type}: {e}")
+            # 寮傚父鏃惰繑鍥炴案鍋囨潯浠讹紝闃叉鏁版嵁娉勯湶
+            conditions.append(QueryCondition(
+                field='id', operator=QueryOperator.EQ, value=-1
+            ))
+            return conditions
 
         if not allowed_ids:
-            print(f"[DataPermFilter] No data permissions configured, allowing all")
+            # 娌℃湁鏁版嵁鏉冮檺閰嶇疆锛屽厑璁告墍鏈?             return conditions
 
         if len(allowed_ids) == 1:
diff --git a/meta/services/data_permission_service.py b/meta/services/data_permission_service.py
index f52038a..6abc124 100644
--- a/meta/services/data_permission_service.py
+++ b/meta/services/data_permission_service.py
@@ -9,10 +9,13 @@
 """
 
 from typing import List, Dict, Any, Optional, Set, Tuple
+import logging
 
 from meta.services.cascade_service import HierarchyConfigLoader
 from meta.core.models import registry
 
+logger = logging.getLogger(__name__)
+
 
 class DataPermissionService:
 
@@ -183,7 +186,7 @@ class DataPermissionService:
                     'permission_level': propagate_level,
                     'reason': 'auto_propagated_from_child'
                 })
-                print(f"[DataPermService] Propagated: {parent_type}({parent_id}) -> read (from {current_type}({current_id}))")
+                logger.debug("Propagated: %s(%s) -> read (from %s(%s))", parent_type, parent_id, current_type, current_id)
             
             current_type = parent_type
             current_id = parent_id
@@ -315,11 +318,11 @@ class DataPermissionService:
             if perm_idx > resource_idx:
                 parent_id = self._find_parent_id(perm['resource_type'], perm['resource_id'], resource_type)
                 if parent_id:
-                    print(f"[DataPermService] Upward visibility: {perm['resource_type']}({perm['resource_id']}) -> {resource_type}({parent_id})")
+                    logger.debug("Upward visibility: %s(%s) -> %s(%s)", perm['resource_type'], perm['resource_id'], resource_type, parent_id)
                     result.add(parent_id)
 
         if result:
-            print(f"[DataPermService] Upward visible {resource_type} IDs: {result}")
+            logger.debug("Upward visible %s IDs: %s", resource_type, result)
         return result
 
     def _get_parent_visibility_permission_level(self, user_id: int, resource_type: str,
@@ -348,12 +351,12 @@ class DataPermissionService:
             for child_id in child_ids:
                 child_level = self._get_direct_permission_level(user_id, child_type, child_id)
                 if child_level:
-                    print(f"[DataPermService] Parent visibility: {resource_type}({resource_id}) -> read (from child {child_type}({child_id}))")
+                    logger.debug("Parent visibility: %s(%s) -> read (from child %s(%s))", resource_type, resource_id, child_type, child_id)
                     return 'read'
                 
                 child_inherited = self._get_inherited_permission_level(user_id, child_type, child_id)
                 if child_inherited:
-                    print(f"[DataPermService] Parent visibility: {resource_type}({resource_id}) -> read (from inherited child {child_type}({child_id}))")
+                    logger.debug("Parent visibility: %s(%s) -> read (from inherited child %s(%s))", resource_type, resource_id, child_type, child_id)
                     return 'read'
         
         return None
@@ -579,7 +582,7 @@ class DataPermissionService:
         for perm in direct_perms:
             perm['source'] = 'direct'
             result.append(perm)
-        print(f"[DataPermService] User {user_id} direct permissions: {len(direct_perms)}")
+        logger.debug("User %s direct permissions: %s", user_id, len(direct_perms))
 
         # 2. 閫氳繃鐢ㄦ埛缁勨啋瑙掕壊鑾峰緱鐨勬潈闄?         cursor = self.ds.execute("""
@@ -595,8 +598,8 @@ class DataPermissionService:
         for perm in group_role_perms:
             perm['source'] = 'group_role'
             result.append(perm)
-        print(f"[DataPermService] User {user_id} group-role permissions: {len(group_role_perms)}")
-        print(f"[DataPermService] User {user_id} total effective permissions: {len(result)}")
+        logger.debug("User %s group-role permissions: %s", user_id, len(group_role_perms))
+        logger.debug("User %s total effective permissions: %s", user_id, len(result))
 
         return result
 
@@ -635,7 +638,7 @@ class DataPermissionService:
         parent_idx = self._get_level_index(parent_type)
         resource_idx = self._get_level_index(resource_type)
 
-        print(f"[DEBUG] _find_parent_id: {resource_type}({resource_id}) -> {parent_type}, indices: {resource_idx} -> {parent_idx}")
+        logger.debug("_find_parent_id: %s(%s) -> %s, indices: %s -> %s", resource_type, resource_id, parent_type, resource_idx, parent_idx)
 
         if parent_idx >= resource_idx:
             return None
@@ -648,7 +651,7 @@ class DataPermissionService:
             fk_field = self._get_parent_fk_field(current_type, parent_type_step)
             table_name = self._get_table_name(current_type)
             
-            print(f"[DEBUG]   Step: {current_type}({current_id}) -> {parent_type_step}, FK: {fk_field}, Table: {table_name}")
+            logger.debug("  Step: %s(%s) -> %s, FK: %s, Table: %s", current_type, current_id, parent_type_step, fk_field, table_name)
             
             if not fk_field:
                 return None
@@ -659,12 +662,12 @@ class DataPermissionService:
             )
             row = cursor.fetchone()
             if not row:
-                print(f"[DEBUG]   No row found!")
+                logger.debug("  No row found!")
                 return None
 
             current_id = row[0]
             current_type = parent_type_step
-            print(f"[DEBUG]   Result: parent_id = {current_id}")
+            logger.debug("  Result: parent_id = %s", current_id)
 
         return current_id
 
@@ -682,6 +685,11 @@ class DataPermissionService:
         return fk
 
     def _get_table_name(self, resource_type: str) -> str:
+        # 浼樺厛浠庡眰绾ч厤缃幏鍙栬〃鍚嶏紙hierarchies.yaml 涓殑 table_name 鏇村彲闈狅級
+        hierarchy_table = self._hierarchy_loader.get_table_name(resource_type)
+        if hierarchy_table:
+            return hierarchy_table
+        # Fallback: 浠?registry 鑾峰彇
         normalized = resource_type.rstrip('s')
         meta_obj = registry.get(normalized)
         if meta_obj and meta_obj.table_name:
@@ -724,7 +732,7 @@ class DataPermissionService:
             row = cursor.fetchone()
             return row[0] if row else None
         except Exception as e:
-            print(f"[DEBUG] add_role_data_permission error: {e}")
+            logger.error("add_role_data_permission error: %s", e)
             return None
 
     def remove_role_data_permission(self, perm_id: int) -> bool:
@@ -811,7 +819,7 @@ class DataPermissionService:
             if row:
                 return {'name': row[0], 'code': row[1]}
         except Exception as e:
-            print(f"[DEBUG] _get_resource_detail error: {e}")
+            logger.error("_get_resource_detail error: %s", e)
         return {}
 
     def _build_resource_path(self, resource_type: str, resource_id: int) -> List[Dict]:
@@ -876,7 +884,7 @@ class DataPermissionService:
                     break
 
             except Exception as e:
-                print(f"[DEBUG] _build_resource_path error: {e}")
+                logger.error("_build_resource_path error: %s", e)
                 break
 
         return path_chain
diff --git a/meta/services/dimension_scope_engine.py b/meta/services/dimension_scope_engine.py
index 6d7ab1a..e6849a9 100644
--- a/meta/services/dimension_scope_engine.py
+++ b/meta/services/dimension_scope_engine.py
@@ -117,9 +117,29 @@ class DimensionScopeEngine:
         expanded = {}
         for scope in scopes:
             code = scope['dimension_code']
-            values = set(json.loads(scope['dimension_values'])
-                         if isinstance(scope['dimension_values'], str)
-                         else scope['dimension_values'])
+            # [FIX 2026-06-15] 鍏煎涓夌鏁版嵁褰㈡€?
+            # 1. dim_values 涓?JSON 瀛楃涓?(涓绘祦, 鏉ヨ嚜 UI 閰嶇疆)
+            # 2. dim_values 涓?list (Python 璋冪敤)
+            # 3. dim_values 涓?NULL (鏃ф暟鎹? id 瀹為檯瀛樺埌 inherit_children)
+            # 淇鍓? dim_values=NULL 鏃?set(None) 鎶?TypeError
+            # 淇鍚? NULL 鏃堕檷绾ц inherit_children (鍚屾牱瀛?JSON 鍒楄〃)
+            raw_dv = scope.get('dimension_values')
+            if raw_dv is None:
+                # [FIX 2026-06-15] Bug #3: NULL 鈫?璇?inherit_children 瀛楁
+                raw_dv = scope.get('inherit_children')
+                if raw_dv is None:
+                    continue
+            if isinstance(raw_dv, str):
+                try:
+                    values = set(json.loads(raw_dv))
+                except (json.JSONDecodeError, TypeError):
+                    continue
+            elif isinstance(raw_dv, (list, tuple)):
+                values = set(int(x) for x in raw_dv if str(x).lstrip('-').isdigit())
+            else:
+                continue
+            if not values:
+                continue
 
             if code not in expanded:
                 expanded[code] = set()
diff --git a/src/components/common/DetailPage/DetailPage.vue b/src/components/common/DetailPage/DetailPage.vue
index ac21755..bcbd052 100644
--- a/src/components/common/DetailPage/DetailPage.vue
+++ b/src/components/common/DetailPage/DetailPage.vue
@@ -378,6 +378,21 @@ const computedFieldDefs = computed(() => {
       }
     }
 
+    // [V1.2.0 2026-06-15] 閫忎紶 dual_mode 鏍囧織
+    // YAML 绀轰緥:
+    //   value_help:
+    //     source: { type: bo, target_bo: business_object, ... }
+    //     dual_mode: true            # 鈫?鍔犺繖涓€琛屽惎鐢?BoSelectorDualMode
+    // 鎴?ui.relation 璺緞:
+    //   ui:
+    //     relation: business_object
+    //     dual_mode: true
+    if (valueHelp && f.value_help?.dual_mode === true) {
+      valueHelp = { ...valueHelp, dual_mode: true }
+    } else if (valueHelp && f.ui?.dual_mode === true) {
+      valueHelp = { ...valueHelp, dual_mode: true }
+    }
+
     defs[f.id] = {
       label: f.name || f.id,
       type: f.type || 'text',
diff --git a/src/components/common/FkLinkField/FkLinkField.vue b/src/components/common/FkLinkField/FkLinkField.vue
index 93d0c52..a7113a2 100644
--- a/src/components/common/FkLinkField/FkLinkField.vue
+++ b/src/components/common/FkLinkField/FkLinkField.vue
@@ -76,7 +76,8 @@ const linkTitle = computed(() => {
 
 <style scoped>
 .fk-link {
-  color: var(--color-primary);
+  /* 浣跨敤鏂囨湰涓昏壊锛堥粦鑹诧級淇濇寔涓庡師鐗堜竴鑷达紱hover 鏃剁敤 primary 寮鸿皟浜や簰 */
+  color: var(--color-text-primary);
   text-decoration: none;
   cursor: pointer;
   transition: color 0.15s ease, text-decoration 0.15s ease;
@@ -89,7 +90,7 @@ const linkTitle = computed(() => {
 }
 
 .fk-link:hover {
-  color: var(--color-primary-hover);
+  color: var(--color-primary);
   text-decoration: underline;
 }
 
diff --git a/src/components/common/MasterDetailLayout/MasterDetailLayout.vue b/src/components/common/MasterDetailLayout/MasterDetailLayout.vue
index e0b65e7..d41d79d 100644
--- a/src/components/common/MasterDetailLayout/MasterDetailLayout.vue
+++ b/src/components/common/MasterDetailLayout/MasterDetailLayout.vue
@@ -1,5 +1,9 @@
 <template>
-  <div class="master-detail-layout" :class="{ 'master-detail-layout--collapsed': isCollapsed }">
+  <div
+    ref="rootRef"
+    class="master-detail-layout"
+    :class="{ 'master-detail-layout--collapsed': isCollapsed, 'master-detail-layout--resizing': isResizing }"
+  >
     <aside
       :class="['master-detail-layout__sidebar', { 'master-detail-layout__sidebar--collapsed': isCollapsed }]"
       :style="sidebarStyle"
@@ -7,44 +11,54 @@
       <div class="master-detail-layout__sidebar-content">
         <slot name="master" />
       </div>
-
-      <button
-        v-if="sidebarCollapsible"
-        class="master-detail-layout__collapse-btn"
-        :class="{ 'master-detail-layout__collapse-btn--collapsed': isCollapsed }"
-        @click="toggleCollapse"
-        :title="isCollapsed ? '灞曞紑渚ц竟鏍? : '鎶樺彔渚ц竟鏍?"
-        type="button"
-      >
-        <svg
-          class="master-detail-layout__collapse-icon"
-          :class="{ 'master-detail-layout__collapse-icon--collapsed': isCollapsed }"
-          viewBox="0 0 24 24"
-          fill="none"
-          stroke="currentColor"
-          stroke-width="2"
-        >
-          <polyline points="15 18 9 12 15 6" />
-        </svg>
-      </button>
     </aside>
 
     <div
       v-if="showBorder && !isCollapsed"
-      :class="['master-detail-layout__resizer', { 'master-detail-layout__resizer--dragging': isResizing }]"
+      ref="resizerRef"
+      class="master-detail-layout__resizer"
+      :class="{ 'master-detail-layout__resizer--dragging': isResizing }"
+      title="宸﹀彸鎷栧姩璋冩暣瀹藉害"
       @mousedown="startResize"
-      title="鎷栧姩璋冩暣瀹藉害"
-    />
+      @touchstart="startResize"
+    >
+      <span class="master-detail-layout__resizer-handle" aria-hidden="true" />
+    </div>
 
     <main class="master-detail-layout__detail">
       <slot name="detail" />
       <slot name="empty" />
     </main>
+
+    <!--
+      鎶樺彔鎸夐挳锛氭斁鍦?layout 鏍圭骇鍒紙涓嶆槸 sidebar 鍐呴儴锛夛紝閬垮厤 sidebar 鎶樺彔 width:0 鏃惰 overflow:hidden 瑁佹帀銆?+      閫氳繃 v-if 鎺у埗娓叉煋锛屾姌鍙犳€佷篃鏄剧ず浠ヤ究鐢ㄦ埛閲嶆柊灞曞紑銆?+    -->
+    <button
+      v-if="sidebarCollapsible"
+      class="master-detail-layout__collapse-btn"
+      :class="{ 'master-detail-layout__collapse-btn--collapsed': isCollapsed }"
+      @click="toggleCollapse"
+      :title="isCollapsed ? '灞曞紑渚ц竟鏍? : '鎶樺彔渚ц竟鏍?"
+      :style="collapseBtnStyle"
+      type="button"
+    >
+      <svg
+        class="master-detail-layout__collapse-icon"
+        :class="{ 'master-detail-layout__collapse-icon--collapsed': isCollapsed }"
+        viewBox="0 0 24 24"
+        fill="none"
+        stroke="currentColor"
+        stroke-width="2"
+      >
+        <polyline points="15 18 9 12 15 6" />
+      </svg>
+    </button>
   </div>
 </template>
 
 <script setup>
-import { ref, computed, onMounted, onUnmounted } from 'vue'
+import { ref, computed, onMounted, onBeforeUnmount, nextTick } from 'vue'
 
 const props = defineProps({
   sidebarWidth: {
@@ -76,8 +90,10 @@ const props = defineProps({
 const emit = defineEmits(['update:sidebarCollapsed', 'collapse-change'])
 
 const isCollapsed = computed(() => props.sidebarCollapsed)
-const currentWidth = ref(parseInt(props.sidebarWidth, 10))
+const currentWidth = ref(parseInt(props.sidebarWidth, 10) || 280)
 const isResizing = ref(false)
+const rootRef = ref(null)
+const resizerRef = ref(null)
 
 const sidebarStyle = computed(() => {
   if (isCollapsed.value) {
@@ -86,45 +102,85 @@ const sidebarStyle = computed(() => {
   return { width: `${currentWidth.value}px` }
 })
 
+/**
+ * 鎶樺彔鎸夐挳浣嶇疆锛氬缁堜綅浜?sidebar 鍙宠竟缂樺渚?+ * - 灞曞紑鎬侊細left = sidebar 瀹藉害锛屽浐瀹氫笉鍔?+ * - 鎶樺彔鎬侊細left = 8px锛堝乏瀵归綈锛夛紝鏂逛究鐢ㄦ埛鎵惧埌
+ */
+const collapseBtnStyle = computed(() => {
+  if (isCollapsed.value) {
+    return { left: '8px' }
+  }
+  return { left: `${currentWidth.value - 12}px` }
+})
+
 function toggleCollapse() {
   const newState = !isCollapsed.value
   emit('update:sidebarCollapsed', newState)
   emit('collapse-change', newState)
 }
 
+function getClientX(event) {
+  if (event.touches && event.touches.length) {
+    return event.touches[0].clientX
+  }
+  if (event.changedTouches && event.changedTouches.length) {
+    return event.changedTouches[0].clientX
+  }
+  return event.clientX
+}
+
+function getRootOffsetLeft() {
+  if (!rootRef.value) return 0
+  const rect = rootRef.value.getBoundingClientRect()
+  return rect && typeof rect.left === 'number' ? rect.left : 0
+}
+
 function startResize(event) {
   if (isCollapsed.value) return
-  
+
   isResizing.value = true
-  document.addEventListener('mousemove', handleResize)
+  document.addEventListener('mousemove', handleResize, { passive: false })
   document.addEventListener('mouseup', stopResize)
+  document.addEventListener('touchmove', handleResize, { passive: false })
+  document.addEventListener('touchend', stopResize)
   document.body.style.cursor = 'col-resize'
   document.body.style.userSelect = 'none'
 }
 
 function handleResize(event) {
   if (!isResizing.value) return
+  if (event && event.cancelable) event.preventDefault()
 
-  const newWidth = event.clientX
-  const clampedWidth = Math.min(props.maxWidth, Math.max(props.minWidth, newWidth))
+  const clientX = getClientX(event)
+  const offsetLeft = getRootOffsetLeft()
+  const newWidth = clientX - offsetLeft
+  const clampedWidth = Math.min(props.maxWidth, Math.max(props.minWidth, Math.round(newWidth)))
   currentWidth.value = clampedWidth
 }
 
 function stopResize() {
+  if (!isResizing.value) return
   isResizing.value = false
   document.removeEventListener('mousemove', handleResize)
   document.removeEventListener('mouseup', stopResize)
+  document.removeEventListener('touchmove', handleResize)
+  document.removeEventListener('touchend', stopResize)
   document.body.style.cursor = ''
   document.body.style.userSelect = ''
+  document.body.style.overflow = ''
 }
 
-onMounted(() => {
-  currentWidth.value = parseInt(props.sidebarWidth, 10)
+onMounted(async () => {
+  await nextTick()
+  const parsed = parseInt(props.sidebarWidth, 10)
+  if (!Number.isNaN(parsed) && parsed > 0) {
+    currentWidth.value = parsed
+  }
 })
 
-onUnmounted(() => {
-  document.removeEventListener('mousemove', handleResize)
-  document.removeEventListener('mouseup', stopResize)
+onBeforeUnmount(() => {
+  stopResize()
 })
 </script>
 
@@ -138,6 +194,11 @@ onUnmounted(() => {
   position: relative;
 }
 
+/* 鎷栨嫿涓細绂佺敤 sidebar 鐨?width transition 閬垮厤瑙嗚寤惰繜 */
+.master-detail-layout--resizing .master-detail-layout__sidebar {
+  transition: none !important;
+}
+
 .master-detail-layout__sidebar {
   flex-shrink: 0;
   display: flex;
@@ -147,6 +208,7 @@ onUnmounted(() => {
   transition: width var(--transition-normal);
   overflow: hidden;
   position: relative;
+  min-width: 0;
 }
 
 .master-detail-layout__sidebar--collapsed {
@@ -160,6 +222,54 @@ onUnmounted(() => {
   min-width: 0;
 }
 
+/* 鎷栨嫿鍒嗛殧鏉★細鍙鐨勬彙鎶?+ 瀹?hit 鍖哄煙 */
+.master-detail-layout__resizer {
+  flex-shrink: 0;
+  width: 12px;
+  /* 璐?margin-left 璁?hit 鍖哄煙璺ㄨ秺 sidebar 杈圭紭 6px锛宧it-test 浣撻獙鏇村ソ */
+  margin-left: -6px;
+  margin-right: -6px;
+  cursor: col-resize;
+  background: transparent;
+  position: relative;
+  z-index: 5;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  /* 璁?hit 鍖哄煙涓嶅洜 margin 鏀剁缉 */
+  pointer-events: auto;
+  touch-action: none;
+}
+
+.master-detail-layout__resizer-handle {
+  width: 2px;
+  height: 32px;
+  background: var(--color-border);
+  border-radius: 1px;
+  transition: background 0.15s ease, height 0.15s ease;
+  pointer-events: none;
+  flex-shrink: 0;
+}
+
+.master-detail-layout__resizer:hover .master-detail-layout__resizer-handle,
+.master-detail-layout__resizer--dragging .master-detail-layout__resizer-handle {
+  background: var(--color-primary);
+  height: 56px;
+}
+
+.master-detail-layout__detail {
+  flex: 1;
+  min-width: 0;
+  overflow: auto;
+  display: flex;
+  flex-direction: column;
+}
+
+/*
+ * 鎶樺彔鎸夐挳锛氭斁鍦?layout 鏍圭骇鍒紝閬垮厤 sidebar 鎶樺彔鏃?overflow:hidden 瑁佸垏
+ * - 灞曞紑鎬侊細left = sidebar.width - 12 (鐩栧湪 sidebar 鍙宠竟绾夸笂)
+ * - 鎶樺彔鎬侊細left = 8px (鐙珛鏄剧ず鍦ㄥ乏杈圭紭)
+ */
 .master-detail-layout__collapse-btn {
   position: absolute;
   top: 50%;
@@ -171,22 +281,23 @@ onUnmounted(() => {
   justify-content: center;
   background: var(--color-bg-container);
   border: 1px solid var(--color-border);
-  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
+  border-radius: var(--radius-sm);
   cursor: pointer;
-  z-index: 10;
+  z-index: 20;
   color: var(--color-text-secondary);
-  transition: all var(--transition-fast), left var(--transition-normal);
-}
-
-.master-detail-layout--collapsed .master-detail-layout__collapse-btn {
-  border-left: 1px solid var(--color-border);
-  border-radius: var(--radius-sm);
-  box-shadow: 2px 0 8px rgba(0, 0, 0, 0.08);
+  transition: background-color 0.15s ease, color 0.15s ease, left var(--transition-normal), border-radius 0.15s ease;
+  padding: 0;
+  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
 }
 
 .master-detail-layout__collapse-btn:hover {
   background: var(--color-bg-secondary);
   color: var(--color-text-primary);
+  z-index: 21;
+}
+
+.master-detail-layout__collapse-btn--collapsed {
+  box-shadow: 2px 0 8px rgba(0, 0, 0, 0.12);
 }
 
 .master-detail-layout__collapse-icon {
@@ -199,31 +310,6 @@ onUnmounted(() => {
   transform: rotate(180deg);
 }
 
-.master-detail-layout__resizer {
-  width: 6px;
-  cursor: col-resize;
-  background: transparent;
-  flex-shrink: 0;
-  position: relative;
-  transition: background var(--transition-fast);
-}
-
-.master-detail-layout__resizer:hover {
-  background: var(--color-primary-light);
-}
-
-.master-detail-layout__resizer:active {
-  background: var(--color-primary);
-}
-
-.master-detail-layout__detail {
-  flex: 1;
-  min-width: 0;
-  overflow: auto;
-  display: flex;
-  flex-direction: column;
-}
-
 @media (max-width: 768px) {
   .master-detail-layout {
     flex-direction: column;
@@ -246,23 +332,15 @@ onUnmounted(() => {
   }
 
   .master-detail-layout__collapse-btn {
-    left: auto;
-    right: 0;
     top: 0;
+    right: 0;
+    left: auto !important;
     transform: none;
-    border-left: 1px solid var(--color-border);
     border-radius: 0 0 0 var(--radius-sm);
-    margin-left: 0;
     width: 48px;
     height: 24px;
   }
 
-  .master-detail-layout--collapsed .master-detail-layout__collapse-btn {
-    left: auto;
-    right: 0;
-    border-radius: var(--radius-sm);
-  }
-
   .master-detail-layout__collapse-icon {
     transform: rotate(-90deg);
   }
diff --git a/src/components/common/MetaForm.vue b/src/components/common/MetaForm.vue
index 862aab2..0cd42ae 100644
--- a/src/components/common/MetaForm.vue
+++ b/src/components/common/MetaForm.vue
@@ -76,7 +76,7 @@
         />
 
         <ValueHelpField
-          v-else-if="field.type === 'value_help' || field.value_help"
+          v-else-if="(field.type === 'value_help' || field.value_help) && !isDualMode(field)"
           v-model="formData[field.key]"
           :value-help-config="field.valueHelpConfig || field.value_help"
           :disabled="field.disabled"
@@ -87,6 +87,23 @@
           @change="validateField(field.key)"
         />
 
+        <!-- [V1.2.0 2026-06-15] 鍏冩暟鎹┍鍔? dual_mode: true 鈫?娓叉煋 BoSelectorDualMode
+             閫傜敤: 璺ㄥ煙鍏崇郴 / 璺ㄥ煙 BO 寮曠敤绛夊満鏅?+             璇﹁ .trae/specs/cross-domain-relationship-permission/spec.md (Option B)
+        -->
+        <BoSelectorDualMode
+          v-else-if="(field.type === 'value_help' || field.value_help) && isDualMode(field)"
+          :model-value="formData[field.key]"
+          :product-id="getProductIdForField(field)"
+          :label="field.label"
+          :required="field.required"
+          :allow-cross-domain="true"
+          :disabled="field.disabled"
+          @update:model-value="val => { formData[field.key] = val }"
+          @cross-domain-toggled="(val) => onDualModeToggle(field.key, val)"
+          @code-error="(err) => onDualModeCodeError(field.key, err)"
+        />
+
         <label v-else-if="field.type === 'checkbox'" class="mf-checkbox">
           <input
             v-model="formData[field.key]"
@@ -120,9 +137,10 @@
 </template>
 
 <script setup>
-import { reactive, computed, watch } from 'vue'
+import { reactive, computed, watch, inject } from 'vue'
 import AppSelect from './AppSelect/AppSelect.vue'
 import ValueHelpField from './ValueHelpField.vue'
+import BoSelectorDualMode from './ValueHelp/BoSelectorDualMode.vue'
 
 const props = defineProps({
   fields: {
@@ -162,7 +180,40 @@ const props = defineProps({
   }
 })
 
-const emit = defineEmits(['update:modelValue', 'change'])
+const emit = defineEmits(['update:modelValue', 'change', 'cross-domain-toggled', 'code-error'])
+
+// [V1.2.0 2026-06-15] 鍏冩暟鎹┍鍔? dual_mode 璇嗗埆
+// 鐖剁粍浠跺彲閫氳繃 provide('productId', ...) 娉ㄥ叆 productId; 缂虹渷鏃跺洖閫€鍒?props.productId
+const providedProductId = inject('productId', null)
+
+function isDualMode(field) {
+  // 妫€鏌ュ绉?value_help 閰嶇疆浣嶇疆
+  const vh = field.valueHelp || field.valueHelpConfig || field.value_help
+  return vh && vh.dual_mode === true
+}
+
+function getProductIdForField(field) {
+  // 浼樺厛绾? field.productId > provided > 璺敱 query > 榛樿 1
+  if (field.productId) return field.productId
+  if (providedProductId?.value) return providedProductId.value
+  if (typeof window !== 'undefined' && window.location) {
+    const url = new URL(window.location.href)
+    const q = url.searchParams.get('productId')
+    if (q) return Number(q)
+  }
+  return 1  // 鍏滃簳榛樿 product
+}
+
+function onDualModeToggle(fieldKey, val) {
+  // 杞彂缁欑埗缁勪欢, 渚涘璁?(B.3 钀藉湴)
+  emit('cross-domain-toggled', { fieldKey, enabled: val, ts: new Date().toISOString() })
+  console.info('[MetaForm] dual-mode cross-domain toggled:', fieldKey, val)
+}
+
+function onDualModeCodeError(fieldKey, errCode) {
+  emit('code-error', { fieldKey, code: errCode, ts: new Date().toISOString() })
+  console.warn('[MetaForm] dual-mode code error:', fieldKey, errCode)
+}
 
 const formData = reactive({})
 const displayValues = reactive({})
diff --git a/src/components/common/MetaListPage/MetaListPage.vue b/src/components/common/MetaListPage/MetaListPage.vue
index 668640f..0bccc6f 100644
--- a/src/components/common/MetaListPage/MetaListPage.vue
+++ b/src/components/common/MetaListPage/MetaListPage.vue
@@ -1957,13 +1957,14 @@ defineExpose({
 }
 
 .bk-link {
-  color: var(--color-primary);
+  /* 涓氬姟閿摼鎺ワ細涓庡師鐗堜竴鑷翠娇鐢ㄩ粦鑹叉枃鏈紝hover 鐢?primary 寮鸿皟 */
+  color: var(--color-text-primary);
   cursor: pointer;
   transition: color 0.15s ease, text-decoration 0.15s ease;
 }
 
 .bk-link:hover {
-  color: var(--color-primary-hover);
+  color: var(--color-primary);
   text-decoration: underline;
 }
 
diff --git a/src/services/boService.js b/src/services/boService.js
index 89f06f9..2daf884 100644
--- a/src/services/boService.js
+++ b/src/services/boService.js
@@ -88,6 +88,37 @@ class BOService extends BaseService {
   searchValueHelp(sourceType, sourceId, params) { return this._searchHelp.searchValueHelp(sourceType, sourceId, params) }
   resolveValueHelp(sourceType, sourceId, value, params) { return this._searchHelp.resolveValueHelp(sourceType, sourceId, value, params) }
 
+  // [V1.2.0 2026-06-15] 璺ㄥ煙鍏崇郴 Pick by Code 妯″紡 (Phase 3)
+  // 鐢ㄤ簬鍙屾ā寮?ValueHelp (List mode 涓嶅 read scope 鈫?鐪嬩笉鍒?D2; Pick by Code 閫冪敓鍙?
+  // 娉? 杩欐槸 BO 鍏冩暟鎹煡璇? 涓嶅彈 read scope 闄愬埗 (鍙牎楠屽瓨鍦ㄦ€?+ product_id)
+  async pickBoByCode(code, productId, options = {}) {
+    if (!code || typeof code !== 'string') {
+      return { success: false, message: 'MISSING_CODE', data: null }
+    }
+    if (!productId || (typeof productId !== 'number' && typeof productId !== 'string')) {
+      return { success: false, message: 'MISSING_PRODUCT_ID', data: null }
+    }
+    const queryParams = new URLSearchParams()
+    queryParams.set('code', code)
+    queryParams.set('product_id', String(productId))
+    if (options.reason) queryParams.set('reason', options.reason)
+    if (options.includeOutOfScope) queryParams.set('include_out_of_scope', 'true')
+    const path = `/bo/business_object/pick_by_code?${queryParams.toString()}`
+    return this._request('GET', path)
+  }
+
+  // [V1.2.0 2026-06-15] 鎸?ID 绮剧‘鏌ヨ BO (渚?Pick by Code 妯″紡鍥為€€浣跨敤)
+  async pickBoById(boId, options = {}) {
+    if (!boId) {
+      return { success: false, message: 'MISSING_BO_ID', data: null }
+    }
+    const queryParams = new URLSearchParams()
+    if (options.reason) queryParams.set('reason', options.reason)
+    const qs = queryParams.toString()
+    const path = `/bo/business_object/${boId}${qs ? '?' + qs : ''}`
+    return this._request('GET', path)
+  }
+
   // 灞傜骇鎿嶄綔 鈥?濮旀墭鍒?hierarchyService锛團R-GAP-004锛?   getHierarchyTree(rootType, params) { return _hierarchyService.getHierarchyTree(rootType, params) }
   getChildCount(objectType, id, params) { return _hierarchyService.getChildCount(objectType, id, params) }
