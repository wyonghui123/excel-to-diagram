test(help): rewrite HelpCenterDrawer tests for P1 iframe embed

- Replace placeholder-button tests with iframe-based assertions:
  - iframe exists in DOM with correct src attribute
  - default helpUrl = /docs/user-guide/index.html
  - custom helpUrl overrides default
  - header new-tab button opens helpUrl in window.open
  - iframe re-keyed on drawer reopen
- Keep stable tests: close/mask click, width prop, body scroll lock, Escape
